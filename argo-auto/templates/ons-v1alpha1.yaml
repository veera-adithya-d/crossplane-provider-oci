
apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: oci-ons-v1alpha1-tests-template
spec:
  serviceAccountName: argo-workflow
  entrypoint: ons-tests-workflow
  volumes:
    - name: git-repo
      persistentVolumeClaim:
        claimName: git-repo-pvc
  arguments:
    parameters:
    - name: compartment_ocid
    - name: create_compartment
      value: "true"
    - name: create_resources
      value: "true"
    - name: delete_resources
      value: "true"
  templates:
    - name: ons-tests-workflow
      dag:
        tasks:
        - name: create-compartment
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_compartment}} == true"
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/identity/v1alpha1/compartment.yaml"
              - name: envVars
                value: "COMPARTMENT_OCID={{workflow.parameters.compartment_ocid}}"
        - name: create-subscription
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          dependencies: [create-compartment, create-notificationtopic]
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/ons/v1alpha1/subscription.yaml"
        - name: create-notificationtopic
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          dependencies: [create-compartment]
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/ons/v1alpha1/notificationtopic.yaml"
        - name: delete-notificationtopic
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-notificationtopic", "delete-subscription"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-notificationtopic.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-notificationtopic.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-notificationtopic.outputs.parameters.resourceGroup}}"
        - name: delete-subscription
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-subscription"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-subscription.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-subscription.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-subscription.outputs.parameters.resourceGroup}}"