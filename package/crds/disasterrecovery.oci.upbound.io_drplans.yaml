---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: drplans.disasterrecovery.oci.upbound.io
spec:
  group: disasterrecovery.oci.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: DrPlan
    listKind: DrPlanList
    plural: drplans
    singular: drplan
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DrPlan is the Schema for the DrPlans API. Provides the Dr Plan
          resource in Oracle Cloud Infrastructure Disaster Recovery service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DrPlanSpec defines the desired state of DrPlan
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace.  Example: {"Operations.CostCenter":
                      "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  displayName:
                    description: '(Updatable) The display name of the DR plan being
                      created.  Example: EBS Switchover PHX to IAD'
                    type: string
                  drProtectionGroupId:
                    description: 'The OCID of the DR protection group to which this
                      DR plan belongs.  Example: ocid1.drprotectiongroup.oc1..uniqueID'
                    type: string
                  drProtectionGroupIdRef:
                    description: Reference to a DrProtectionGroup in disasterrecovery
                      to populate drProtectionGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  drProtectionGroupIdSelector:
                    description: Selector for a DrProtectionGroup in disasterrecovery
                      to populate drProtectionGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Simple key-value pair that is applied
                      without any predefined name, type or scope. Exists for cross-compatibility
                      only.  Example: {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  refreshTrigger:
                    description: (Updatable) An optional property when incremented
                      triggers Refresh. Could be set to any integer value.
                    type: number
                  sourcePlanId:
                    description: 'The OCID of the source DR plan that should be cloned.  Example:
                      ocid1.drplan.oc1..uniqueID'
                    type: string
                  type:
                    description: The type of DR plan to be created.
                    type: string
                  verifyTrigger:
                    description: (Updatable) An optional property when incremented
                      triggers Verify. Could be set to any integer value.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace.  Example: {"Operations.CostCenter":
                      "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  displayName:
                    description: '(Updatable) The display name of the DR plan being
                      created.  Example: EBS Switchover PHX to IAD'
                    type: string
                  drProtectionGroupId:
                    description: 'The OCID of the DR protection group to which this
                      DR plan belongs.  Example: ocid1.drprotectiongroup.oc1..uniqueID'
                    type: string
                  drProtectionGroupIdRef:
                    description: Reference to a DrProtectionGroup in disasterrecovery
                      to populate drProtectionGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  drProtectionGroupIdSelector:
                    description: Selector for a DrProtectionGroup in disasterrecovery
                      to populate drProtectionGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Simple key-value pair that is applied
                      without any predefined name, type or scope. Exists for cross-compatibility
                      only.  Example: {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  refreshTrigger:
                    description: (Updatable) An optional property when incremented
                      triggers Refresh. Could be set to any integer value.
                    type: number
                  sourcePlanId:
                    description: 'The OCID of the source DR plan that should be cloned.  Example:
                      ocid1.drplan.oc1..uniqueID'
                    type: string
                  type:
                    description: The type of DR plan to be created.
                    type: string
                  verifyTrigger:
                    description: (Updatable) An optional property when incremented
                      triggers Verify. Could be set to any integer value.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.displayName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.displayName)
                || (has(self.initProvider) && has(self.initProvider.displayName))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: DrPlanStatus defines the observed state of DrPlan.
            properties:
              atProvider:
                properties:
                  compartmentId:
                    description: 'The OCID of the compartment containing the DR plan.  Example:
                      ocid1.compartment.oc1..uniqueID'
                    type: string
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace.  Example: {"Operations.CostCenter":
                      "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  displayName:
                    description: '(Updatable) The display name of the DR plan being
                      created.  Example: EBS Switchover PHX to IAD'
                    type: string
                  drProtectionGroupId:
                    description: 'The OCID of the DR protection group to which this
                      DR plan belongs.  Example: ocid1.drprotectiongroup.oc1..uniqueID'
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Simple key-value pair that is applied
                      without any predefined name, type or scope. Exists for cross-compatibility
                      only.  Example: {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: 'The OCID of the DR plan.  Example: ocid1.drplan.oc1..uniqueID'
                    type: string
                  lifeCycleDetails:
                    description: A message describing the DR plan's current state
                      in more detail.
                    type: string
                  lifecycleSubState:
                    description: The current state of the DR plan.
                    type: string
                  peerDrProtectionGroupId:
                    description: 'The OCID of the peer DR protection group associated
                      with this plan''s DR protection group.  Example: ocid1.drprotectiongroup.oc1..uniqueID'
                    type: string
                  peerRegion:
                    description: 'The region of the peer DR protection group associated
                      with this plan''s DR protection group.  Example: us-ashburn-1'
                    type: string
                  planGroups:
                    description: The list of groups in this DR plan.
                    items:
                      properties:
                        displayName:
                          description: '(Updatable) The display name of the DR plan
                            being created.  Example: EBS Switchover PHX to IAD'
                          type: string
                        id:
                          description: 'The OCID of the DR plan.  Example: ocid1.drplan.oc1..uniqueID'
                          type: string
                        isPauseEnabled:
                          description: 'A flag indicating whether this group should
                            be enabled for execution. This flag is only applicable
                            to the USER_DEFINED_PAUSE group. The flag should be null
                            for the remaining group types.  Example: true'
                          type: boolean
                        refreshStatus:
                          description: 'The DR plan group refresh status.  Example:
                            GROUP_MODIFIED'
                          type: string
                        steps:
                          description: The list of steps in the group.
                          items:
                            properties:
                              displayName:
                                description: '(Updatable) The display name of the
                                  DR plan being created.  Example: EBS Switchover
                                  PHX to IAD'
                                type: string
                              errorMode:
                                description: The error mode for this step.
                                type: string
                              groupId:
                                description: 'The unique id of the group to which
                                  this step belongs. Must not be modified by user.  Example:
                                  sgid1.group..uniqueID'
                                type: string
                              id:
                                description: 'The OCID of the DR plan.  Example: ocid1.drplan.oc1..uniqueID'
                                type: string
                              isEnabled:
                                description: 'A flag indicating whether this step
                                  should be enabled for execution.  Example: true'
                                type: boolean
                              memberId:
                                description: 'The OCID of the member associated with
                                  this step.  Example: ocid1.database.oc1..uniqueID'
                                type: string
                              refreshStatus:
                                description: 'The DR plan group refresh status.  Example:
                                  GROUP_MODIFIED'
                                type: string
                              timeout:
                                description: 'The timeout in seconds for executing
                                  this step.  Example: 600'
                                type: number
                              type:
                                description: The type of DR plan to be created.
                                type: string
                              typeDisplayName:
                                description: 'The display name of the DR Plan step
                                  type.  Example: Database Switchover'
                                type: string
                              userDefinedStep:
                                description: The details for a user-defined step in
                                  a DR plan.
                                items:
                                  properties:
                                    functionId:
                                      description: 'The OCID of function to be invoked.  Example:
                                        ocid1.fnfunc.oc1..uniqueID'
                                      type: string
                                    functionRegion:
                                      description: 'The region in which the function
                                        is deployed.  Example: us-ashburn-1'
                                      type: string
                                    objectStorageScriptLocation:
                                      description: The details of an object storage
                                        script location for a user-defined step in
                                        a DR plan.
                                      items:
                                        properties:
                                          bucket:
                                            description: 'The bucket name inside the
                                              object storage namespace.  Example:
                                              custom_dr_scripts'
                                            type: string
                                          namespace:
                                            description: 'The namespace in object
                                              storage (Note - this is usually the
                                              tenancy name).  Example: myocitenancy'
                                            type: string
                                          object:
                                            description: 'The object name inside the
                                              object storage bucket.  Example: validate_app_start.sh'
                                            type: string
                                        type: object
                                      type: array
                                    requestBody:
                                      description: 'The request body for the function.  Example:
                                        { "FnParam1", "FnParam2" }'
                                      type: string
                                    runAsUser:
                                      description: 'The userid on the instance to
                                        be used for executing the script or command.  Example:
                                        opc'
                                      type: string
                                    runOnInstanceId:
                                      description: The OCID of the instance on which
                                        this script or command should be executed.
                                      type: string
                                    runOnInstanceRegion:
                                      description: 'The region of the instance where
                                        this script or command should be executed.  Example:
                                        us-ashburn-1'
                                      type: string
                                    scriptCommand:
                                      description: 'The script name and arguments.  Example:
                                        /usr/bin/python3 /home/opc/scripts/my_app_script.py
                                        arg1 arg2 arg3'
                                      type: string
                                    stepType:
                                      description: The type of the user-defined step.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        type:
                          description: The type of DR plan to be created.
                          type: string
                      type: object
                    type: array
                  refreshTrigger:
                    description: (Updatable) An optional property when incremented
                      triggers Refresh. Could be set to any integer value.
                    type: number
                  sourcePlanId:
                    description: 'The OCID of the source DR plan that should be cloned.  Example:
                      ocid1.drplan.oc1..uniqueID'
                    type: string
                  state:
                    description: The current state of the DR plan.
                    type: string
                  systemTags:
                    additionalProperties:
                      type: string
                    description: 'Usage of system tag keys. These predefined keys
                      are scoped to namespaces.  Example: {"orcl-cloud.free-tier-retained":
                      "true"}'
                    type: object
                    x-kubernetes-map-type: granular
                  timeCreated:
                    description: 'The date and time the DR plan was created. An RFC3339
                      formatted datetime string.  Example: 2019-03-29T09:36:42Z'
                    type: string
                  timeUpdated:
                    description: 'The date and time the DR plan was updated. An RFC3339
                      formatted datetime string.  Example: 2019-03-29T09:36:42Z'
                    type: string
                  type:
                    description: The type of DR plan to be created.
                    type: string
                  verifyTrigger:
                    description: (Updatable) An optional property when incremented
                      triggers Verify. Could be set to any integer value.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
