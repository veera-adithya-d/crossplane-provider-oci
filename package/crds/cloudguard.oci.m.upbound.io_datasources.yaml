---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasources.cloudguard.oci.m.upbound.io
spec:
  group: cloudguard.oci.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: DataSource
    listKind: DataSourceList
    plural: datasources
    singular: datasource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSource is the Schema for the DataSources API. Provides the
          Data Source resource in Oracle Cloud Infrastructure Cloud Guard service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourceSpec defines the desired state of DataSource
            properties:
              forProvider:
                properties:
                  compartmentId:
                    description: (Updatable) Compartment OCID of the data source
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dataSourceDetails:
                    description: (Updatable) Details specific to the data source type.
                    items:
                      properties:
                        additionalEntitiesCount:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) The additional entities count used for data
                            source query
                          type: number
                        dataSourceFeedProvider:
                          description: (Updatable) Type of data source feed provider
                            (LoggingQuery)
                          type: string
                        description:
                          description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                            (Updatable) Description text for the query
                          type: string
                        intervalInMinutes:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Interval in minutes that query is run periodically.
                          type: number
                        intervalInSeconds:
                          description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                            (Updatable) Interval in minutes which query is run periodically.
                          type: number
                        loggingQueryDetails:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Details for a logging query for a data source.
                          items:
                            properties:
                              keyEntitiesCount:
                                description: (Updatable) The key entities count used
                                  for data source query
                                type: number
                              loggingQueryType:
                                description: (Updatable) Logging query type for data
                                  source
                                type: string
                            type: object
                          type: array
                        loggingQueryType:
                          description: (Updatable) Logging query type for data source
                          type: string
                        operator:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Operator used in data source
                          type: string
                        query:
                          description: (Updatable) The continuous query expression
                            that is run periodically.
                          type: string
                        queryStartTime:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Start policy for continuous query
                          items:
                            properties:
                              queryStartTime:
                                description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                                  (Updatable) Start policy for continuous query
                                type: string
                              startPolicyType:
                                description: (Updatable) Start policy delay timing
                                type: string
                            type: object
                          type: array
                        regions:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) List of logging query regions
                          items:
                            type: string
                          type: array
                        scheduledQueryScopeDetails:
                          description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                            (Updatable) Target information in which scheduled query
                            will be run
                          items:
                            properties:
                              region:
                                description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                                  (Updatable) region on which scheduled query needs
                                  to be run
                                type: string
                              resourceIds:
                                description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                                  (Updatable) List of OCIDs on scheduled query needs
                                  to run
                                items:
                                  type: string
                                type: array
                              resourceType:
                                description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                                  (Updatable) Type of resource
                                type: string
                            type: object
                          type: array
                        threshold:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) The integer value that must be exceeded, fall
                            below or equal to (depending on the operator), for the
                            query result to trigger an event
                          type: number
                      type: object
                    type: array
                  dataSourceFeedProvider:
                    description: (Updatable) Type of data source feed provider (LoggingQuery)
                    type: string
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. Example: {"foo-namespace.bar-key":
                      "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  displayName:
                    description: (Updatable) Data source display name
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Simple key-value pair that is applied
                      without any predefined name, type or scope. Exists for cross-compatibility
                      only. Example: {"bar-key": "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  status:
                    description: (Updatable) Enablement status of data source.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  compartmentId:
                    description: (Updatable) Compartment OCID of the data source
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dataSourceDetails:
                    description: (Updatable) Details specific to the data source type.
                    items:
                      properties:
                        additionalEntitiesCount:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) The additional entities count used for data
                            source query
                          type: number
                        dataSourceFeedProvider:
                          description: (Updatable) Type of data source feed provider
                            (LoggingQuery)
                          type: string
                        description:
                          description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                            (Updatable) Description text for the query
                          type: string
                        intervalInMinutes:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Interval in minutes that query is run periodically.
                          type: number
                        intervalInSeconds:
                          description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                            (Updatable) Interval in minutes which query is run periodically.
                          type: number
                        loggingQueryDetails:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Details for a logging query for a data source.
                          items:
                            properties:
                              keyEntitiesCount:
                                description: (Updatable) The key entities count used
                                  for data source query
                                type: number
                              loggingQueryType:
                                description: (Updatable) Logging query type for data
                                  source
                                type: string
                            type: object
                          type: array
                        loggingQueryType:
                          description: (Updatable) Logging query type for data source
                          type: string
                        operator:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Operator used in data source
                          type: string
                        query:
                          description: (Updatable) The continuous query expression
                            that is run periodically.
                          type: string
                        queryStartTime:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Start policy for continuous query
                          items:
                            properties:
                              queryStartTime:
                                description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                                  (Updatable) Start policy for continuous query
                                type: string
                              startPolicyType:
                                description: (Updatable) Start policy delay timing
                                type: string
                            type: object
                          type: array
                        regions:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) List of logging query regions
                          items:
                            type: string
                          type: array
                        scheduledQueryScopeDetails:
                          description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                            (Updatable) Target information in which scheduled query
                            will be run
                          items:
                            properties:
                              region:
                                description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                                  (Updatable) region on which scheduled query needs
                                  to be run
                                type: string
                              resourceIds:
                                description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                                  (Updatable) List of OCIDs on scheduled query needs
                                  to run
                                items:
                                  type: string
                                type: array
                              resourceType:
                                description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                                  (Updatable) Type of resource
                                type: string
                            type: object
                          type: array
                        threshold:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) The integer value that must be exceeded, fall
                            below or equal to (depending on the operator), for the
                            query result to trigger an event
                          type: number
                      type: object
                    type: array
                  dataSourceFeedProvider:
                    description: (Updatable) Type of data source feed provider (LoggingQuery)
                    type: string
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. Example: {"foo-namespace.bar-key":
                      "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  displayName:
                    description: (Updatable) Data source display name
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Simple key-value pair that is applied
                      without any predefined name, type or scope. Exists for cross-compatibility
                      only. Example: {"bar-key": "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  status:
                    description: (Updatable) Enablement status of data source.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.dataSourceFeedProvider is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dataSourceFeedProvider)
                || (has(self.initProvider) && has(self.initProvider.dataSourceFeedProvider))'
            - message: spec.forProvider.displayName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.displayName)
                || (has(self.initProvider) && has(self.initProvider.displayName))'
          status:
            description: DataSourceStatus defines the observed state of DataSource.
            properties:
              atProvider:
                properties:
                  compartmentId:
                    description: (Updatable) Compartment OCID of the data source
                    type: string
                  dataSourceDetails:
                    description: (Updatable) Details specific to the data source type.
                    items:
                      properties:
                        additionalEntitiesCount:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) The additional entities count used for data
                            source query
                          type: number
                        dataSourceFeedProvider:
                          description: (Updatable) Type of data source feed provider
                            (LoggingQuery)
                          type: string
                        description:
                          description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                            (Updatable) Description text for the query
                          type: string
                        intervalInMinutes:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Interval in minutes that query is run periodically.
                          type: number
                        intervalInSeconds:
                          description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                            (Updatable) Interval in minutes which query is run periodically.
                          type: number
                        loggingQueryDetails:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Details for a logging query for a data source.
                          items:
                            properties:
                              keyEntitiesCount:
                                description: (Updatable) The key entities count used
                                  for data source query
                                type: number
                              loggingQueryType:
                                description: (Updatable) Logging query type for data
                                  source
                                type: string
                            type: object
                          type: array
                        loggingQueryType:
                          description: (Updatable) Logging query type for data source
                          type: string
                        operator:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Operator used in data source
                          type: string
                        query:
                          description: (Updatable) The continuous query expression
                            that is run periodically.
                          type: string
                        queryStartTime:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) Start policy for continuous query
                          items:
                            properties:
                              queryStartTime:
                                description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                                  (Updatable) Start policy for continuous query
                                type: string
                              startPolicyType:
                                description: (Updatable) Start policy delay timing
                                type: string
                            type: object
                          type: array
                        regions:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) List of logging query regions
                          items:
                            type: string
                          type: array
                        scheduledQueryScopeDetails:
                          description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                            (Updatable) Target information in which scheduled query
                            will be run
                          items:
                            properties:
                              region:
                                description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                                  (Updatable) region on which scheduled query needs
                                  to be run
                                type: string
                              resourceIds:
                                description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                                  (Updatable) List of OCIDs on scheduled query needs
                                  to run
                                items:
                                  type: string
                                type: array
                              resourceType:
                                description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                                  (Updatable) Type of resource
                                type: string
                            type: object
                          type: array
                        threshold:
                          description: (Applicable when data_source_feed_provider=LOGGINGQUERY)
                            (Updatable) The integer value that must be exceeded, fall
                            below or equal to (depending on the operator), for the
                            query result to trigger an event
                          type: number
                      type: object
                    type: array
                  dataSourceDetectorMappingInfo:
                    description: Information about the detector recipe and rule attached
                    items:
                      properties:
                        detectorRecipeId:
                          description: ID of the detector recipe attached to the data
                            source
                          type: string
                        detectorRuleId:
                          description: ID of the detector rule attached to the data
                            source
                          type: string
                      type: object
                    type: array
                  dataSourceFeedProvider:
                    description: (Updatable) Type of data source feed provider (LoggingQuery)
                    type: string
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. Example: {"foo-namespace.bar-key":
                      "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  displayName:
                    description: (Updatable) Data source display name
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Simple key-value pair that is applied
                      without any predefined name, type or scope. Exists for cross-compatibility
                      only. Example: {"bar-key": "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: OCID for the data source
                    type: string
                  regionStatusDetail:
                    description: Information about the region and status of query
                      replication
                    items:
                      properties:
                        region:
                          description: (Applicable when data_source_feed_provider=SCHEDULEDQUERY)
                            (Updatable) region on which scheduled query needs to be
                            run
                          type: string
                        status:
                          description: (Updatable) Enablement status of data source.
                          type: string
                      type: object
                    type: array
                  state:
                    description: The current lifecycle state of the resource.
                    type: string
                  status:
                    description: (Updatable) Enablement status of data source.
                    type: string
                  systemTags:
                    additionalProperties:
                      type: string
                    description: 'System tags for this resource. Each key is predefined
                      and scoped to a namespace. For more information, see Resource
                      Tags. System tags can be viewed by users, but can only be created
                      by the system.  Example: {"orcl-cloud.free-tier-retained": "true"}'
                    type: object
                    x-kubernetes-map-type: granular
                  timeCreated:
                    description: The date and time the Data source was created. Format
                      defined by RFC3339.
                    type: string
                  timeUpdated:
                    description: The date and time the data source was updated. Format
                      defined by RFC3339.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
