---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: autonomousdatabasebackups.database.oci.m.upbound.io
spec:
  group: database.oci.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: AutonomousDatabaseBackup
    listKind: AutonomousDatabaseBackupList
    plural: autonomousdatabasebackups
    singular: autonomousdatabasebackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AutonomousDatabaseBackup is the Schema for the AutonomousDatabaseBackups
          API. Provides the Autonomous Database Backup resource in Oracle Cloud Infrastructure
          Database service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AutonomousDatabaseBackupSpec defines the desired state of
              AutonomousDatabaseBackup
            properties:
              forProvider:
                properties:
                  autonomousDatabaseId:
                    description: The OCID of the Autonomous AI Database backup.
                    type: string
                  autonomousDatabaseIdRef:
                    description: Reference to a AutonomousDatabase in database to
                      populate autonomousDatabaseId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  autonomousDatabaseIdSelector:
                    description: Selector for a AutonomousDatabase in database to
                      populate autonomousDatabaseId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  backupDestinationDetails:
                    description: Backup destination details
                    items:
                      properties:
                        backupRetentionPolicyOnTerminate:
                          description: Defines the automatic and manual backup retention
                            policy for the Autonomous Database termination.  The retention
                            policy set on the Autonomous Container Database is not
                            applicable for cross region remote backups and backups
                            hosted on recovery Appliance backup destination. Options
                            are 'RETAIN_PER_RETENTION_WINDOW' or 'RETAIN_FOR_72_HOURS'.The
                            default value is 'RETAIN_FOR_72_HOURS'.
                          type: string
                        id:
                          description: The OCID of the backup destination.
                          type: string
                        internetProxy:
                          description: Proxy URL to connect to object store.
                          type: string
                        isRemote:
                          description: Indicates whether the backup destination is
                            cross-region or local.
                          type: boolean
                        isRetentionLockEnabled:
                          description: Indicates if backup retention is locked for
                            all the database backups in the Autonomous Container Database
                            (ACD). The retention window cannot be decreased if the
                            backup retention lock is enabled. Once applied on the
                            Autonomous Container Database, the retention lock cannot
                            be removed, or the retention period cannot be decreased
                            after a 14-day period. If the backup is a Long Term Backup
                            and retention lock is enabled, the backup cannot be deleted
                            and must expire. The retention lock set on the Autonomous
                            Container Database is not applicable for cross region
                            remote backups and backups hosted on recovery Appliance
                            backup destination.
                          type: boolean
                        remoteRegion:
                          description: The name of the remote region where the remote
                            automatic incremental backups will be stored.           For
                            information about valid region names, see Regions and
                            Availability Domains.
                          type: string
                        type:
                          description: Type of the database backup destination.
                          type: string
                        vpcPasswordSecretRef:
                          description: For a RECOVERY_APPLIANCE backup destination,
                            the password for the VPC user that is used to access the
                            Recovery Appliance.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        vpcUser:
                          description: For a RECOVERY_APPLIANCE backup destination,
                            the Virtual Private Catalog (VPC) user that is used to
                            access the Recovery Appliance.
                          type: string
                      type: object
                    type: array
                  displayName:
                    description: The user-friendly name for the backup. The name does
                      not have to be unique.
                    type: string
                  isLongTermBackup:
                    description: Indicates whether the backup is long-term
                    type: boolean
                  retentionPeriodInDays:
                    description: (Updatable) Retention period, in days, for long-term
                      backups
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autonomousDatabaseId:
                    description: The OCID of the Autonomous AI Database backup.
                    type: string
                  autonomousDatabaseIdRef:
                    description: Reference to a AutonomousDatabase in database to
                      populate autonomousDatabaseId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  autonomousDatabaseIdSelector:
                    description: Selector for a AutonomousDatabase in database to
                      populate autonomousDatabaseId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  backupDestinationDetails:
                    description: Backup destination details
                    items:
                      properties:
                        backupRetentionPolicyOnTerminate:
                          description: Defines the automatic and manual backup retention
                            policy for the Autonomous Database termination.  The retention
                            policy set on the Autonomous Container Database is not
                            applicable for cross region remote backups and backups
                            hosted on recovery Appliance backup destination. Options
                            are 'RETAIN_PER_RETENTION_WINDOW' or 'RETAIN_FOR_72_HOURS'.The
                            default value is 'RETAIN_FOR_72_HOURS'.
                          type: string
                        id:
                          description: The OCID of the backup destination.
                          type: string
                        internetProxy:
                          description: Proxy URL to connect to object store.
                          type: string
                        isRemote:
                          description: Indicates whether the backup destination is
                            cross-region or local.
                          type: boolean
                        isRetentionLockEnabled:
                          description: Indicates if backup retention is locked for
                            all the database backups in the Autonomous Container Database
                            (ACD). The retention window cannot be decreased if the
                            backup retention lock is enabled. Once applied on the
                            Autonomous Container Database, the retention lock cannot
                            be removed, or the retention period cannot be decreased
                            after a 14-day period. If the backup is a Long Term Backup
                            and retention lock is enabled, the backup cannot be deleted
                            and must expire. The retention lock set on the Autonomous
                            Container Database is not applicable for cross region
                            remote backups and backups hosted on recovery Appliance
                            backup destination.
                          type: boolean
                        remoteRegion:
                          description: The name of the remote region where the remote
                            automatic incremental backups will be stored.           For
                            information about valid region names, see Regions and
                            Availability Domains.
                          type: string
                        type:
                          description: Type of the database backup destination.
                          type: string
                        vpcPasswordSecretRef:
                          description: For a RECOVERY_APPLIANCE backup destination,
                            the password for the VPC user that is used to access the
                            Recovery Appliance.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        vpcUser:
                          description: For a RECOVERY_APPLIANCE backup destination,
                            the Virtual Private Catalog (VPC) user that is used to
                            access the Recovery Appliance.
                          type: string
                      type: object
                    type: array
                  displayName:
                    description: The user-friendly name for the backup. The name does
                      not have to be unique.
                    type: string
                  isLongTermBackup:
                    description: Indicates whether the backup is long-term
                    type: boolean
                  retentionPeriodInDays:
                    description: (Updatable) Retention period, in days, for long-term
                      backups
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: AutonomousDatabaseBackupStatus defines the observed state
              of AutonomousDatabaseBackup.
            properties:
              atProvider:
                properties:
                  autonomousDatabaseId:
                    description: The OCID of the Autonomous AI Database backup.
                    type: string
                  backupDestinationDetails:
                    description: Backup destination details
                    items:
                      properties:
                        backupRetentionPolicyOnTerminate:
                          description: Defines the automatic and manual backup retention
                            policy for the Autonomous Database termination.  The retention
                            policy set on the Autonomous Container Database is not
                            applicable for cross region remote backups and backups
                            hosted on recovery Appliance backup destination. Options
                            are 'RETAIN_PER_RETENTION_WINDOW' or 'RETAIN_FOR_72_HOURS'.The
                            default value is 'RETAIN_FOR_72_HOURS'.
                          type: string
                        id:
                          description: The OCID of the backup destination.
                          type: string
                        internetProxy:
                          description: Proxy URL to connect to object store.
                          type: string
                        isRemote:
                          description: Indicates whether the backup destination is
                            cross-region or local.
                          type: boolean
                        isRetentionLockEnabled:
                          description: Indicates if backup retention is locked for
                            all the database backups in the Autonomous Container Database
                            (ACD). The retention window cannot be decreased if the
                            backup retention lock is enabled. Once applied on the
                            Autonomous Container Database, the retention lock cannot
                            be removed, or the retention period cannot be decreased
                            after a 14-day period. If the backup is a Long Term Backup
                            and retention lock is enabled, the backup cannot be deleted
                            and must expire. The retention lock set on the Autonomous
                            Container Database is not applicable for cross region
                            remote backups and backups hosted on recovery Appliance
                            backup destination.
                          type: boolean
                        remoteRegion:
                          description: The name of the remote region where the remote
                            automatic incremental backups will be stored.           For
                            information about valid region names, see Regions and
                            Availability Domains.
                          type: string
                        type:
                          description: Type of the database backup destination.
                          type: string
                        vpcUser:
                          description: For a RECOVERY_APPLIANCE backup destination,
                            the Virtual Private Catalog (VPC) user that is used to
                            access the Recovery Appliance.
                          type: string
                      type: object
                    type: array
                  compartmentId:
                    description: The OCID of the compartment.
                    type: string
                  databaseSizeInTbs:
                    description: The size of the database in terabytes at the time
                      the backup was taken.
                    type: number
                  dbVersion:
                    description: A valid Oracle AI Database version for Autonomous
                      AI Database.
                    type: string
                  displayName:
                    description: The user-friendly name for the backup. The name does
                      not have to be unique.
                    type: string
                  id:
                    description: The OCID of the backup destination.
                    type: string
                  infrastructureType:
                    description: The infrastructure type this resource belongs to.
                    type: string
                  isAutomatic:
                    description: Indicates whether the backup is user-initiated or
                      automatic.
                    type: boolean
                  isLongTermBackup:
                    description: Indicates whether the backup is long-term
                    type: boolean
                  isRestorable:
                    description: Indicates whether the backup can be used to restore
                      the associated Autonomous AI Database.
                    type: boolean
                  keyStoreId:
                    description: The OCID of the key store of Oracle Vault.
                    type: string
                  keyStoreWalletName:
                    description: The wallet name for Oracle Key Vault.
                    type: string
                  kmsKeyId:
                    description: The OCID of the key container that is used as the
                      master encryption key in database transparent data encryption
                      (TDE) operations.
                    type: string
                  kmsKeyVersionId:
                    description: The OCID of the key container version that is used
                      in database transparent data encryption (TDE) operations KMS
                      Key can have multiple key versions. If none is specified, the
                      current key version (latest) of the Key Id is used for the operation.
                      Autonomous AI Database Serverless does not use key versions,
                      hence is not applicable for Autonomous AI Database Serverless
                      instances.
                    type: string
                  lifecycleDetails:
                    description: Additional information about the current lifecycle
                      state.
                    type: string
                  region:
                    description: Name of the region in which backup is taken in.
                    type: string
                  retentionPeriodInDays:
                    description: (Updatable) Retention period, in days, for long-term
                      backups
                    type: number
                  sizeInTbs:
                    description: The backup size in terrabytes (TB).
                    type: number
                  sourceDatabaseDetails:
                    description: Source Autonomous Database details.
                    items:
                      properties:
                        autonomousContainerDatabaseCustomerContacts:
                          description: Customer Contacts for the Autonomous Container
                            Database. Setting this to an empty list removes all customer
                            contacts.
                          items:
                            properties:
                              email:
                                description: The email address used by Oracle to send
                                  notifications regarding databases and infrastructure.
                                type: string
                            type: object
                          type: array
                        autonomousContainerDatabaseDisplayName:
                          description: The user-provided name for the Autonomous Container
                            Database.
                          type: string
                        autonomousContainerDatabaseDstFileVersion:
                          description: DST Time-Zone File version of the Autonomous
                            Container Database.
                          type: string
                        autonomousContainerDatabaseName:
                          description: Autonomous Container Database name.
                          type: string
                        autonomousDatabaseCustomerContacts:
                          description: Customer Contacts for the Autonomous Database.
                          items:
                            properties:
                              email:
                                description: The email address used by Oracle to send
                                  notifications regarding databases and infrastructure.
                                type: string
                            type: object
                          type: array
                        autonomousDatabaseName:
                          description: Autonomous Database's name.
                          type: string
                        autonomousVmClusterDisplayName:
                          description: Autonomous VM cluster's user-friendly name.
                          type: string
                        dbWorkload:
                          description: 'The Autonomous Database workload type. The
                            following values are valid:'
                          type: string
                      type: object
                    type: array
                  state:
                    description: The current state of the backup.
                    type: string
                  timeAvailableTill:
                    description: Timestamp until when the backup will be available
                    type: string
                  timeEnded:
                    description: The date and time the backup completed.
                    type: string
                  timeStarted:
                    description: The date and time the backup started.
                    type: string
                  type:
                    description: Type of the database backup destination.
                    type: string
                  vaultId:
                    description: The OCID of the Oracle Cloud Infrastructure vault.
                      This parameter and secretId are required for Customer Managed
                      Keys.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
