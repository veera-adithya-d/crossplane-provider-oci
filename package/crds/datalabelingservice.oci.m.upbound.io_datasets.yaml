---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasets.datalabelingservice.oci.m.upbound.io
spec:
  group: datalabelingservice.oci.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: Dataset
    listKind: DatasetList
    plural: datasets
    singular: dataset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Dataset is the Schema for the Datasets API. Provides the Dataset
          resource in Oracle Cloud Infrastructure Data Labeling Service service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DatasetSpec defines the desired state of Dataset
            properties:
              forProvider:
                properties:
                  annotationFormat:
                    description: The annotation format name required for labeling
                      records.
                    type: string
                  compartmentId:
                    description: (Updatable) The OCID of the compartment of the resource.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasetFormatDetails:
                    description: It specifies how to process the data. Supported formats
                      include DOCUMENT, IMAGE, and TEXT.
                    items:
                      properties:
                        formatType:
                          description: The format type. DOCUMENT format is for record
                            contents that are PDFs or TIFFs. IMAGE format is for record
                            contents that are JPEGs or PNGs. TEXT format is for record
                            contents that are TXT files.
                          type: string
                        textFileTypeMetadata:
                          description: (Applicable when format_type=TEXT) Metadata
                            for files with text content.
                          items:
                            properties:
                              columnDelimiter:
                                description: A column delimiter
                                type: string
                              columnIndex:
                                description: The index of a selected column. This
                                  is a zero-based index.
                                type: number
                              columnName:
                                description: The name of a selected column.
                                type: string
                              escapeCharacter:
                                description: An escape character.
                                type: string
                              formatType:
                                description: The format type. DOCUMENT format is for
                                  record contents that are PDFs or TIFFs. IMAGE format
                                  is for record contents that are JPEGs or PNGs. TEXT
                                  format is for record contents that are TXT files.
                                type: string
                              lineDelimiter:
                                description: A line delimiter.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  datasetSourceDetails:
                    description: This allows the customer to specify the source of
                      the dataset.
                    items:
                      properties:
                        bucket:
                          description: The object storage bucket that contains the
                            dataset data source.
                          type: string
                        namespace:
                          description: The namespace of the bucket that contains the
                            dataset data source.
                          type: string
                        prefix:
                          description: A common path prefix shared by the objects
                            that make up the dataset. Except for the CSV file type,
                            records are not generated for the objects whose names
                            exactly match with the prefix.
                          type: string
                        sourceType:
                          description: The source type. OBJECT_STORAGE allows the
                            user to describe where in object storage the dataset is.
                          type: string
                      type: object
                    type: array
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) The defined tags for this resource.
                      Each key is predefined and scoped to a namespace. For example:
                      {"foo-namespace": {"bar-key": "value"}}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) A user provided description of the dataset
                    type: string
                  displayName:
                    description: (Updatable) A user-friendly display name for the
                      resource.
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) A simple key-value pair that is applied
                      without any predefined name, type, or scope. It exists for cross-compatibility
                      only. For example: {"bar-key": "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  initialImportDatasetConfiguration:
                    description: Initial import dataset configuration. Allows user
                      to create dataset from existing dataset files.
                    items:
                      properties:
                        importFormat:
                          description: File format details used for importing dataset
                          items:
                            properties:
                              name:
                                description: Name of import format
                                type: string
                              version:
                                description: Version of import format
                                type: string
                            type: object
                          type: array
                        importMetadataPath:
                          description: Object storage path for the metadata file
                          items:
                            properties:
                              bucket:
                                description: The object storage bucket that contains
                                  the dataset data source.
                                type: string
                              namespace:
                                description: The namespace of the bucket that contains
                                  the dataset data source.
                                type: string
                              path:
                                description: Path for the metadata file.
                                type: string
                              sourceType:
                                description: The source type. OBJECT_STORAGE allows
                                  the user to describe where in object storage the
                                  dataset is.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  initialRecordGenerationConfiguration:
                    description: The initial generate records configuration. It generates
                      records from the dataset's source.
                    items:
                      type: object
                    type: array
                  labelSet:
                    description: An ordered collection of labels that are unique by
                      name.
                    items:
                      properties:
                        items:
                          description: An ordered collection of labels that are unique
                            by name.
                          items:
                            properties:
                              name:
                                description: Name of import format
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  labelingInstructions:
                    description: (Updatable) The labeling instructions for human labelers
                      in rich text format
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  annotationFormat:
                    description: The annotation format name required for labeling
                      records.
                    type: string
                  compartmentId:
                    description: (Updatable) The OCID of the compartment of the resource.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasetFormatDetails:
                    description: It specifies how to process the data. Supported formats
                      include DOCUMENT, IMAGE, and TEXT.
                    items:
                      properties:
                        formatType:
                          description: The format type. DOCUMENT format is for record
                            contents that are PDFs or TIFFs. IMAGE format is for record
                            contents that are JPEGs or PNGs. TEXT format is for record
                            contents that are TXT files.
                          type: string
                        textFileTypeMetadata:
                          description: (Applicable when format_type=TEXT) Metadata
                            for files with text content.
                          items:
                            properties:
                              columnDelimiter:
                                description: A column delimiter
                                type: string
                              columnIndex:
                                description: The index of a selected column. This
                                  is a zero-based index.
                                type: number
                              columnName:
                                description: The name of a selected column.
                                type: string
                              escapeCharacter:
                                description: An escape character.
                                type: string
                              formatType:
                                description: The format type. DOCUMENT format is for
                                  record contents that are PDFs or TIFFs. IMAGE format
                                  is for record contents that are JPEGs or PNGs. TEXT
                                  format is for record contents that are TXT files.
                                type: string
                              lineDelimiter:
                                description: A line delimiter.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  datasetSourceDetails:
                    description: This allows the customer to specify the source of
                      the dataset.
                    items:
                      properties:
                        bucket:
                          description: The object storage bucket that contains the
                            dataset data source.
                          type: string
                        namespace:
                          description: The namespace of the bucket that contains the
                            dataset data source.
                          type: string
                        prefix:
                          description: A common path prefix shared by the objects
                            that make up the dataset. Except for the CSV file type,
                            records are not generated for the objects whose names
                            exactly match with the prefix.
                          type: string
                        sourceType:
                          description: The source type. OBJECT_STORAGE allows the
                            user to describe where in object storage the dataset is.
                          type: string
                      type: object
                    type: array
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) The defined tags for this resource.
                      Each key is predefined and scoped to a namespace. For example:
                      {"foo-namespace": {"bar-key": "value"}}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) A user provided description of the dataset
                    type: string
                  displayName:
                    description: (Updatable) A user-friendly display name for the
                      resource.
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) A simple key-value pair that is applied
                      without any predefined name, type, or scope. It exists for cross-compatibility
                      only. For example: {"bar-key": "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  initialImportDatasetConfiguration:
                    description: Initial import dataset configuration. Allows user
                      to create dataset from existing dataset files.
                    items:
                      properties:
                        importFormat:
                          description: File format details used for importing dataset
                          items:
                            properties:
                              name:
                                description: Name of import format
                                type: string
                              version:
                                description: Version of import format
                                type: string
                            type: object
                          type: array
                        importMetadataPath:
                          description: Object storage path for the metadata file
                          items:
                            properties:
                              bucket:
                                description: The object storage bucket that contains
                                  the dataset data source.
                                type: string
                              namespace:
                                description: The namespace of the bucket that contains
                                  the dataset data source.
                                type: string
                              path:
                                description: Path for the metadata file.
                                type: string
                              sourceType:
                                description: The source type. OBJECT_STORAGE allows
                                  the user to describe where in object storage the
                                  dataset is.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  initialRecordGenerationConfiguration:
                    description: The initial generate records configuration. It generates
                      records from the dataset's source.
                    items:
                      type: object
                    type: array
                  labelSet:
                    description: An ordered collection of labels that are unique by
                      name.
                    items:
                      properties:
                        items:
                          description: An ordered collection of labels that are unique
                            by name.
                          items:
                            properties:
                              name:
                                description: Name of import format
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  labelingInstructions:
                    description: (Updatable) The labeling instructions for human labelers
                      in rich text format
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.annotationFormat is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.annotationFormat)
                || (has(self.initProvider) && has(self.initProvider.annotationFormat))'
            - message: spec.forProvider.datasetFormatDetails is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasetFormatDetails)
                || (has(self.initProvider) && has(self.initProvider.datasetFormatDetails))'
            - message: spec.forProvider.datasetSourceDetails is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasetSourceDetails)
                || (has(self.initProvider) && has(self.initProvider.datasetSourceDetails))'
            - message: spec.forProvider.labelSet is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.labelSet)
                || (has(self.initProvider) && has(self.initProvider.labelSet))'
          status:
            description: DatasetStatus defines the observed state of Dataset.
            properties:
              atProvider:
                properties:
                  additionalProperties:
                    additionalProperties:
                      type: string
                    description: 'A simple key-value pair that is applied without
                      any predefined name, type, or scope. It exists for cross-compatibility
                      only. For example: {"bar-key": "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  annotationFormat:
                    description: The annotation format name required for labeling
                      records.
                    type: string
                  compartmentId:
                    description: (Updatable) The OCID of the compartment of the resource.
                    type: string
                  datasetFormatDetails:
                    description: It specifies how to process the data. Supported formats
                      include DOCUMENT, IMAGE, and TEXT.
                    items:
                      properties:
                        formatType:
                          description: The format type. DOCUMENT format is for record
                            contents that are PDFs or TIFFs. IMAGE format is for record
                            contents that are JPEGs or PNGs. TEXT format is for record
                            contents that are TXT files.
                          type: string
                        textFileTypeMetadata:
                          description: (Applicable when format_type=TEXT) Metadata
                            for files with text content.
                          items:
                            properties:
                              columnDelimiter:
                                description: A column delimiter
                                type: string
                              columnIndex:
                                description: The index of a selected column. This
                                  is a zero-based index.
                                type: number
                              columnName:
                                description: The name of a selected column.
                                type: string
                              escapeCharacter:
                                description: An escape character.
                                type: string
                              formatType:
                                description: The format type. DOCUMENT format is for
                                  record contents that are PDFs or TIFFs. IMAGE format
                                  is for record contents that are JPEGs or PNGs. TEXT
                                  format is for record contents that are TXT files.
                                type: string
                              lineDelimiter:
                                description: A line delimiter.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  datasetSourceDetails:
                    description: This allows the customer to specify the source of
                      the dataset.
                    items:
                      properties:
                        bucket:
                          description: The object storage bucket that contains the
                            dataset data source.
                          type: string
                        namespace:
                          description: The namespace of the bucket that contains the
                            dataset data source.
                          type: string
                        prefix:
                          description: A common path prefix shared by the objects
                            that make up the dataset. Except for the CSV file type,
                            records are not generated for the objects whose names
                            exactly match with the prefix.
                          type: string
                        sourceType:
                          description: The source type. OBJECT_STORAGE allows the
                            user to describe where in object storage the dataset is.
                          type: string
                      type: object
                    type: array
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) The defined tags for this resource.
                      Each key is predefined and scoped to a namespace. For example:
                      {"foo-namespace": {"bar-key": "value"}}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) A user provided description of the dataset
                    type: string
                  displayName:
                    description: (Updatable) A user-friendly display name for the
                      resource.
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) A simple key-value pair that is applied
                      without any predefined name, type, or scope. It exists for cross-compatibility
                      only. For example: {"bar-key": "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: The OCID of the Dataset.
                    type: string
                  initialImportDatasetConfiguration:
                    description: Initial import dataset configuration. Allows user
                      to create dataset from existing dataset files.
                    items:
                      properties:
                        importFormat:
                          description: File format details used for importing dataset
                          items:
                            properties:
                              name:
                                description: Name of import format
                                type: string
                              version:
                                description: Version of import format
                                type: string
                            type: object
                          type: array
                        importMetadataPath:
                          description: Object storage path for the metadata file
                          items:
                            properties:
                              bucket:
                                description: The object storage bucket that contains
                                  the dataset data source.
                                type: string
                              namespace:
                                description: The namespace of the bucket that contains
                                  the dataset data source.
                                type: string
                              path:
                                description: Path for the metadata file.
                                type: string
                              sourceType:
                                description: The source type. OBJECT_STORAGE allows
                                  the user to describe where in object storage the
                                  dataset is.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  initialRecordGenerationConfiguration:
                    description: The initial generate records configuration. It generates
                      records from the dataset's source.
                    items:
                      type: object
                    type: array
                  labelSet:
                    description: An ordered collection of labels that are unique by
                      name.
                    items:
                      properties:
                        items:
                          description: An ordered collection of labels that are unique
                            by name.
                          items:
                            properties:
                              name:
                                description: Name of import format
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  labelingInstructions:
                    description: (Updatable) The labeling instructions for human labelers
                      in rich text format
                    type: string
                  lifecycleDetails:
                    description: A message describing the current state in more detail.
                      For example, it can be used to provide actionable information
                      for a resource in FAILED or NEEDS_ATTENTION state.
                    type: string
                  lifecycleSubstate:
                    description: The sub-state of the dataset. IMPORT_DATASET - The
                      dataset is being imported.
                    type: string
                  state:
                    description: The state of a dataset. CREATING - The dataset is
                      being created.  It will transition to ACTIVE when it is ready
                      for labeling. ACTIVE   - The dataset is ready for labeling.
                      UPDATING - The dataset is being updated.  It and its related
                      resources may be unavailable for other updates until it returns
                      to ACTIVE. NEEDS_ATTENTION - A dataset updation operation has
                      failed due to validation or other errors and needs attention.
                      DELETING - The dataset and its related resources are being deleted.
                      DELETED  - The dataset has been deleted and is no longer available.
                      FAILED   - The dataset has failed due to validation or other
                      errors.
                    type: string
                  timeCreated:
                    description: The date and time the resource was created, in the
                      timestamp format defined by RFC3339.
                    type: string
                  timeUpdated:
                    description: The date and time the resource was last updated,
                      in the timestamp format defined by RFC3339.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
