---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: domainsidentitypropagationtrusts.identity.oci.upbound.io
spec:
  group: identity.oci.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: DomainsIdentityPropagationTrust
    listKind: DomainsIdentityPropagationTrustList
    plural: domainsidentitypropagationtrusts
    singular: domainsidentitypropagationtrust
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DomainsIdentityPropagationTrust is the Schema for the DomainsIdentityPropagationTrusts
          API. Provides the Identity Propagation Trust resource in Oracle Cloud Infrastructure
          Identity Domains service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DomainsIdentityPropagationTrustSpec defines the desired state
              of DomainsIdentityPropagationTrust
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accountId:
                    description: (Updatable) The Identity cloud provider service identifier,
                      for example, the Azure Tenancy ID, AWS Account ID, or GCP Project
                      ID.
                    type: string
                  active:
                    description: (Updatable) If true, specifies that this Identity
                      Propagation Trust is in an enabled state. The default value
                      is false.
                    type: boolean
                  allowImpersonation:
                    description: (Updatable) Allow customers to define whether the
                      resulting token should contain the authenticated user as the
                      subject or whether the token should impersonate another Application
                      Principal in IAM.
                    type: boolean
                  attributeSets:
                    description: (Updatable) A multi-valued list of strings indicating
                      the return type of attribute definition. The specified set of
                      attributes can be fetched by the return type of the attribute.
                      One or more values can be given together to fetch more than
                      one group of attributes. If 'attributes' query parameter is
                      also available, union of the two is fetched. Valid values -
                      all, always, never, request, default. Values are case-insensitive.
                    items:
                      type: string
                    type: array
                  attributes:
                    description: (Updatable) A comma-delimited string that specifies
                      the names of resource attributes that should be returned in
                      the response. By default, a response that contains resource
                      attributes contains only attributes that are defined in the
                      schema for that resource type as returned=always or returned=default.
                      An attribute that is defined as returned=request is returned
                      in a response only if the request specifies its name in the
                      value of this query parameter. If a request specifies this query
                      parameter, the response contains the attributes that this query
                      parameter specifies, as well as any attribute that is defined
                      as returned=always.
                    type: string
                  authorization:
                    description: (Updatable) The Authorization field value consists
                      of credentials containing the authentication information of
                      the user agent for the realm of the resource being requested.
                    type: string
                  clientClaimName:
                    description: (Updatable) The claim name that identifies to whom
                      the JWT/SAML token is issued. If AWS, then "aud" or "client_id".
                      If Azure, then "appid". If GCP, then "aud".
                    type: string
                  clientClaimValues:
                    description: (Updatable) The value that corresponds to the client
                      claim name used to identify to whom the token is issued.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  clockSkewSeconds:
                    description: (Updatable) The clock skew (in secs) that's allowed
                      for the token issue and expiry time.
                    type: number
                  description:
                    description: (Updatable) The description of the Identity Propagation
                      Trust.
                    type: string
                  idcsEndpoint:
                    description: The basic endpoint for the identity domain
                    type: string
                  impersonationServiceUsers:
                    description: (Updatable) The Impersonating Principal.
                    items:
                      properties:
                        ocid:
                          description: (Updatable) The OCID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                        rule:
                          description: (Updatable) The rule expression to be used
                            for matching the inbound token for impersonation.
                          type: string
                        value:
                          description: (Updatable) The ID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                        valueRef:
                          description: Reference to a DomainsUser in identity to populate
                            value.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        valueSelector:
                          description: Selector for a DomainsUser in identity to populate
                            value.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  issuer:
                    description: (Updatable) The issuer claim of the Identity provider.
                    type: string
                  keytab:
                    description: (Updatable) The keytab stored in the tenancy's Vault.
                      This is required if the identity propagation type is 'SPNEGO'.
                    items:
                      properties:
                        secretOcid:
                          description: (Updatable) The OCID of the secret. The secret
                            content corresponding to the OCID is expected to be in
                            Base64 encoded content type.
                          type: string
                        secretVersion:
                          description: (Updatable) The version of the secret. When
                            the version is not specified, then the latest secret version
                            is used during runtime.
                          type: number
                      type: object
                    type: array
                  name:
                    description: The name of the the Identity Propagation Trust.
                    type: string
                  oauthClients:
                    description: (Updatable) The value of all the authorized OAuth
                      Clients.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ocid:
                    description: (Updatable) The OCID of the SCIM resource that represents
                      the User or App who created this Resource
                    type: string
                  publicCertificate:
                    description: (Updatable) Store the public key if public key cert.
                    type: string
                  publicKeyEndpoint:
                    description: (Updatable) The cloud provider's public key API of
                      SAML and OIDC providers for signature validation.
                    type: string
                  resourceTypeSchemaVersion:
                    description: (Updatable) An endpoint-specific schema version number
                      to use in the Request. Allowed version values are Earliest Version
                      or Latest Version as specified in each REST API endpoint description,
                      or any sequential number inbetween. All schema attributes/body
                      parameters are a part of version 1. After version 1, any attributes
                      added or deprecated will be tagged with the version that they
                      were added to or deprecated in. If no version is provided, the
                      latest schema version is returned.
                    type: string
                  schemas:
                    description: (Updatable) REQUIRED. The schemas attribute is an
                      array of Strings which allows introspection of the supported
                      schema version for a SCIM representation as well any schema
                      extensions supported by that representation. Each String value
                      must be a unique URI. This specification defines URIs for User,
                      Group, and a standard "enterprise" extension. All representations
                      of SCIM schema MUST include a non-zero value array with value(s)
                      of the URIs supported by that representation. Duplicate values
                      MUST NOT be included. Value order is not specified and MUST
                      not impact behavior.
                    items:
                      type: string
                    type: array
                  subjectClaimName:
                    description: (Updatable) Used for locating the subject claim from
                      the incoming token.
                    type: string
                  subjectMappingAttribute:
                    description: (Updatable) Subject Mapping Attribute to which the
                      value from subject claim name value would be used for identity
                      lookup.
                    type: string
                  subjectType:
                    description: (Updatable) The type of the resource against which
                      lookup will be made in the identity domain in IAM for the incoming
                      subject claim value.
                    type: string
                  tags:
                    description: (Updatable) A list of tags on this resource.
                    items:
                      properties:
                        key:
                          description: (Updatable) Key or name of the tag.
                          type: string
                        value:
                          description: (Updatable) The ID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                      type: object
                    type: array
                  type:
                    description: (Updatable) The type of resource, User or App, that
                      created this Resource
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accountId:
                    description: (Updatable) The Identity cloud provider service identifier,
                      for example, the Azure Tenancy ID, AWS Account ID, or GCP Project
                      ID.
                    type: string
                  active:
                    description: (Updatable) If true, specifies that this Identity
                      Propagation Trust is in an enabled state. The default value
                      is false.
                    type: boolean
                  allowImpersonation:
                    description: (Updatable) Allow customers to define whether the
                      resulting token should contain the authenticated user as the
                      subject or whether the token should impersonate another Application
                      Principal in IAM.
                    type: boolean
                  attributeSets:
                    description: (Updatable) A multi-valued list of strings indicating
                      the return type of attribute definition. The specified set of
                      attributes can be fetched by the return type of the attribute.
                      One or more values can be given together to fetch more than
                      one group of attributes. If 'attributes' query parameter is
                      also available, union of the two is fetched. Valid values -
                      all, always, never, request, default. Values are case-insensitive.
                    items:
                      type: string
                    type: array
                  attributes:
                    description: (Updatable) A comma-delimited string that specifies
                      the names of resource attributes that should be returned in
                      the response. By default, a response that contains resource
                      attributes contains only attributes that are defined in the
                      schema for that resource type as returned=always or returned=default.
                      An attribute that is defined as returned=request is returned
                      in a response only if the request specifies its name in the
                      value of this query parameter. If a request specifies this query
                      parameter, the response contains the attributes that this query
                      parameter specifies, as well as any attribute that is defined
                      as returned=always.
                    type: string
                  authorization:
                    description: (Updatable) The Authorization field value consists
                      of credentials containing the authentication information of
                      the user agent for the realm of the resource being requested.
                    type: string
                  clientClaimName:
                    description: (Updatable) The claim name that identifies to whom
                      the JWT/SAML token is issued. If AWS, then "aud" or "client_id".
                      If Azure, then "appid". If GCP, then "aud".
                    type: string
                  clientClaimValues:
                    description: (Updatable) The value that corresponds to the client
                      claim name used to identify to whom the token is issued.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  clockSkewSeconds:
                    description: (Updatable) The clock skew (in secs) that's allowed
                      for the token issue and expiry time.
                    type: number
                  description:
                    description: (Updatable) The description of the Identity Propagation
                      Trust.
                    type: string
                  idcsEndpoint:
                    description: The basic endpoint for the identity domain
                    type: string
                  impersonationServiceUsers:
                    description: (Updatable) The Impersonating Principal.
                    items:
                      properties:
                        ocid:
                          description: (Updatable) The OCID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                        rule:
                          description: (Updatable) The rule expression to be used
                            for matching the inbound token for impersonation.
                          type: string
                        value:
                          description: (Updatable) The ID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                        valueRef:
                          description: Reference to a DomainsUser in identity to populate
                            value.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        valueSelector:
                          description: Selector for a DomainsUser in identity to populate
                            value.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  issuer:
                    description: (Updatable) The issuer claim of the Identity provider.
                    type: string
                  keytab:
                    description: (Updatable) The keytab stored in the tenancy's Vault.
                      This is required if the identity propagation type is 'SPNEGO'.
                    items:
                      properties:
                        secretOcid:
                          description: (Updatable) The OCID of the secret. The secret
                            content corresponding to the OCID is expected to be in
                            Base64 encoded content type.
                          type: string
                        secretVersion:
                          description: (Updatable) The version of the secret. When
                            the version is not specified, then the latest secret version
                            is used during runtime.
                          type: number
                      type: object
                    type: array
                  name:
                    description: The name of the the Identity Propagation Trust.
                    type: string
                  oauthClients:
                    description: (Updatable) The value of all the authorized OAuth
                      Clients.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ocid:
                    description: (Updatable) The OCID of the SCIM resource that represents
                      the User or App who created this Resource
                    type: string
                  publicCertificate:
                    description: (Updatable) Store the public key if public key cert.
                    type: string
                  publicKeyEndpoint:
                    description: (Updatable) The cloud provider's public key API of
                      SAML and OIDC providers for signature validation.
                    type: string
                  resourceTypeSchemaVersion:
                    description: (Updatable) An endpoint-specific schema version number
                      to use in the Request. Allowed version values are Earliest Version
                      or Latest Version as specified in each REST API endpoint description,
                      or any sequential number inbetween. All schema attributes/body
                      parameters are a part of version 1. After version 1, any attributes
                      added or deprecated will be tagged with the version that they
                      were added to or deprecated in. If no version is provided, the
                      latest schema version is returned.
                    type: string
                  schemas:
                    description: (Updatable) REQUIRED. The schemas attribute is an
                      array of Strings which allows introspection of the supported
                      schema version for a SCIM representation as well any schema
                      extensions supported by that representation. Each String value
                      must be a unique URI. This specification defines URIs for User,
                      Group, and a standard "enterprise" extension. All representations
                      of SCIM schema MUST include a non-zero value array with value(s)
                      of the URIs supported by that representation. Duplicate values
                      MUST NOT be included. Value order is not specified and MUST
                      not impact behavior.
                    items:
                      type: string
                    type: array
                  subjectClaimName:
                    description: (Updatable) Used for locating the subject claim from
                      the incoming token.
                    type: string
                  subjectMappingAttribute:
                    description: (Updatable) Subject Mapping Attribute to which the
                      value from subject claim name value would be used for identity
                      lookup.
                    type: string
                  subjectType:
                    description: (Updatable) The type of the resource against which
                      lookup will be made in the identity domain in IAM for the incoming
                      subject claim value.
                    type: string
                  tags:
                    description: (Updatable) A list of tags on this resource.
                    items:
                      properties:
                        key:
                          description: (Updatable) Key or name of the tag.
                          type: string
                        value:
                          description: (Updatable) The ID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                      type: object
                    type: array
                  type:
                    description: (Updatable) The type of resource, User or App, that
                      created this Resource
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.idcsEndpoint is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.idcsEndpoint)
                || (has(self.initProvider) && has(self.initProvider.idcsEndpoint))'
            - message: spec.forProvider.issuer is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.issuer)
                || (has(self.initProvider) && has(self.initProvider.issuer))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.schemas is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.schemas)
                || (has(self.initProvider) && has(self.initProvider.schemas))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: DomainsIdentityPropagationTrustStatus defines the observed
              state of DomainsIdentityPropagationTrust.
            properties:
              atProvider:
                properties:
                  accountId:
                    description: (Updatable) The Identity cloud provider service identifier,
                      for example, the Azure Tenancy ID, AWS Account ID, or GCP Project
                      ID.
                    type: string
                  active:
                    description: (Updatable) If true, specifies that this Identity
                      Propagation Trust is in an enabled state. The default value
                      is false.
                    type: boolean
                  allowImpersonation:
                    description: (Updatable) Allow customers to define whether the
                      resulting token should contain the authenticated user as the
                      subject or whether the token should impersonate another Application
                      Principal in IAM.
                    type: boolean
                  attributeSets:
                    description: (Updatable) A multi-valued list of strings indicating
                      the return type of attribute definition. The specified set of
                      attributes can be fetched by the return type of the attribute.
                      One or more values can be given together to fetch more than
                      one group of attributes. If 'attributes' query parameter is
                      also available, union of the two is fetched. Valid values -
                      all, always, never, request, default. Values are case-insensitive.
                    items:
                      type: string
                    type: array
                  attributes:
                    description: (Updatable) A comma-delimited string that specifies
                      the names of resource attributes that should be returned in
                      the response. By default, a response that contains resource
                      attributes contains only attributes that are defined in the
                      schema for that resource type as returned=always or returned=default.
                      An attribute that is defined as returned=request is returned
                      in a response only if the request specifies its name in the
                      value of this query parameter. If a request specifies this query
                      parameter, the response contains the attributes that this query
                      parameter specifies, as well as any attribute that is defined
                      as returned=always.
                    type: string
                  authorization:
                    description: (Updatable) The Authorization field value consists
                      of credentials containing the authentication information of
                      the user agent for the realm of the resource being requested.
                    type: string
                  clientClaimName:
                    description: (Updatable) The claim name that identifies to whom
                      the JWT/SAML token is issued. If AWS, then "aud" or "client_id".
                      If Azure, then "appid". If GCP, then "aud".
                    type: string
                  clientClaimValues:
                    description: (Updatable) The value that corresponds to the client
                      claim name used to identify to whom the token is issued.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  clockSkewSeconds:
                    description: (Updatable) The clock skew (in secs) that's allowed
                      for the token issue and expiry time.
                    type: number
                  compartmentOcid:
                    description: (Updatable) Oracle Cloud Infrastructure Compartment
                      Id (ocid) in which the resource lives.
                    type: string
                  deleteInProgress:
                    description: (Updatable) A boolean flag indicating this resource
                      in the process of being deleted. Usually set to true when synchronous
                      deletion of the resource would take too long.
                    type: boolean
                  description:
                    description: (Updatable) The description of the Identity Propagation
                      Trust.
                    type: string
                  domainOcid:
                    description: (Updatable) Oracle Cloud Infrastructure Domain Id
                      (ocid) in which the resource lives.
                    type: string
                  id:
                    description: '(Updatable) Unique identifier for the SCIM Resource
                      as defined by the Service Provider. Each representation of the
                      Resource MUST include a non-empty id value. This identifier
                      MUST be unique across the Service Provider''s entire set of
                      Resources. It MUST be a stable, non-reassignable identifier
                      that does not change when the same Resource is returned in subsequent
                      requests. The value of the id attribute is always issued by
                      the Service Provider and MUST never be specified by the Service
                      Consumer. bulkId: is a reserved keyword and MUST NOT be used
                      in the unique identifier.'
                    type: string
                  idcsCreatedBy:
                    description: (Updatable) The User or App who created the Resource
                    items:
                      properties:
                        display:
                          description: (Updatable) The displayName of the User or
                            App who created this Resource
                          type: string
                        ocid:
                          description: (Updatable) The OCID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                        ref:
                          type: string
                        type:
                          description: (Updatable) The type of resource, User or App,
                            that created this Resource
                          type: string
                        value:
                          description: (Updatable) The ID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                      type: object
                    type: array
                  idcsEndpoint:
                    description: The basic endpoint for the identity domain
                    type: string
                  idcsLastModifiedBy:
                    description: (Updatable) The User or App who modified the Resource
                    items:
                      properties:
                        display:
                          description: (Updatable) The displayName of the User or
                            App who created this Resource
                          type: string
                        ocid:
                          description: (Updatable) The OCID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                        ref:
                          type: string
                        type:
                          description: (Updatable) The type of resource, User or App,
                            that created this Resource
                          type: string
                        value:
                          description: (Updatable) The ID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                      type: object
                    type: array
                  idcsLastUpgradedInRelease:
                    description: (Updatable) The release number when the resource
                      was upgraded.
                    type: string
                  idcsPreventedOperations:
                    description: (Updatable) Each value of this attribute specifies
                      an operation that only an internal client may perform on this
                      particular resource.
                    items:
                      type: string
                    type: array
                  impersonationServiceUsers:
                    description: (Updatable) The Impersonating Principal.
                    items:
                      properties:
                        ocid:
                          description: (Updatable) The OCID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                        ref:
                          type: string
                        rule:
                          description: (Updatable) The rule expression to be used
                            for matching the inbound token for impersonation.
                          type: string
                        value:
                          description: (Updatable) The ID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                      type: object
                    type: array
                  issuer:
                    description: (Updatable) The issuer claim of the Identity provider.
                    type: string
                  keytab:
                    description: (Updatable) The keytab stored in the tenancy's Vault.
                      This is required if the identity propagation type is 'SPNEGO'.
                    items:
                      properties:
                        secretOcid:
                          description: (Updatable) The OCID of the secret. The secret
                            content corresponding to the OCID is expected to be in
                            Base64 encoded content type.
                          type: string
                        secretVersion:
                          description: (Updatable) The version of the secret. When
                            the version is not specified, then the latest secret version
                            is used during runtime.
                          type: number
                      type: object
                    type: array
                  meta:
                    description: (Updatable) A complex attribute that contains resource
                      metadata. All sub-attributes are OPTIONAL.
                    items:
                      properties:
                        created:
                          description: (Updatable) The DateTime the Resource was added
                            to the Service Provider
                          type: string
                        lastModified:
                          description: (Updatable) The most recent DateTime that the
                            details of this Resource were updated at the Service Provider.
                            If this Resource has never been modified since its initial
                            creation, the value MUST be the same as the value of created.
                            The attribute MUST be a DateTime.
                          type: string
                        location:
                          description: (Updatable) The URI of the Resource being returned.
                            This value MUST be the same as the Location HTTP response
                            header.
                          type: string
                        resourceType:
                          description: (Updatable) Name of the resource type of the
                            resource--for example, Users or Groups
                          type: string
                        version:
                          description: (Updatable) The version of the Resource being
                            returned. This value must be the same as the ETag HTTP
                            response header.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The name of the the Identity Propagation Trust.
                    type: string
                  oauthClients:
                    description: (Updatable) The value of all the authorized OAuth
                      Clients.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  ocid:
                    description: (Updatable) The OCID of the SCIM resource that represents
                      the User or App who created this Resource
                    type: string
                  publicCertificate:
                    description: (Updatable) Store the public key if public key cert.
                    type: string
                  publicKeyEndpoint:
                    description: (Updatable) The cloud provider's public key API of
                      SAML and OIDC providers for signature validation.
                    type: string
                  resourceTypeSchemaVersion:
                    description: (Updatable) An endpoint-specific schema version number
                      to use in the Request. Allowed version values are Earliest Version
                      or Latest Version as specified in each REST API endpoint description,
                      or any sequential number inbetween. All schema attributes/body
                      parameters are a part of version 1. After version 1, any attributes
                      added or deprecated will be tagged with the version that they
                      were added to or deprecated in. If no version is provided, the
                      latest schema version is returned.
                    type: string
                  schemas:
                    description: (Updatable) REQUIRED. The schemas attribute is an
                      array of Strings which allows introspection of the supported
                      schema version for a SCIM representation as well any schema
                      extensions supported by that representation. Each String value
                      must be a unique URI. This specification defines URIs for User,
                      Group, and a standard "enterprise" extension. All representations
                      of SCIM schema MUST include a non-zero value array with value(s)
                      of the URIs supported by that representation. Duplicate values
                      MUST NOT be included. Value order is not specified and MUST
                      not impact behavior.
                    items:
                      type: string
                    type: array
                  subjectClaimName:
                    description: (Updatable) Used for locating the subject claim from
                      the incoming token.
                    type: string
                  subjectMappingAttribute:
                    description: (Updatable) Subject Mapping Attribute to which the
                      value from subject claim name value would be used for identity
                      lookup.
                    type: string
                  subjectType:
                    description: (Updatable) The type of the resource against which
                      lookup will be made in the identity domain in IAM for the incoming
                      subject claim value.
                    type: string
                  tags:
                    description: (Updatable) A list of tags on this resource.
                    items:
                      properties:
                        key:
                          description: (Updatable) Key or name of the tag.
                          type: string
                        value:
                          description: (Updatable) The ID of the SCIM resource that
                            represents the User or App who created this Resource
                          type: string
                      type: object
                    type: array
                  tenancyOcid:
                    description: (Updatable) Oracle Cloud Infrastructure Tenant Id
                      (ocid) in which the resource lives.
                    type: string
                  type:
                    description: (Updatable) The type of resource, User or App, that
                      created this Resource
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
