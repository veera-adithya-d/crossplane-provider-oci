---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: publications.marketplace.oci.m.upbound.io
spec:
  group: marketplace.oci.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: Publication
    listKind: PublicationList
    plural: publications
    singular: publication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Publication is the Schema for the Publications API. Provides
          the Publication resource in Oracle Cloud Infrastructure Marketplace service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PublicationSpec defines the desired state of Publication
            properties:
              forProvider:
                properties:
                  compartmentId:
                    description: (Updatable) The OCID of the compartment where you
                      want to create the publication.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) The defined tags associated with this
                      resource, if any. Each key is predefined and scoped to namespaces.
                      For more information, see Resource Tags. Example: {"Operations.CostCenter":
                      "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) The freeform tags associated with this
                      resource, if any. Each tag is a simple key-value pair with no
                      predefined name, type, or namespace. For more information, see
                      Resource Tags. Example: {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  isAgreementAcknowledged:
                    description: Whether the publisher acknowledged that they have
                      the right and authority to share the contents of the publication
                      and that they accepted the Oracle terms of use agreements required
                      to create a publication.
                    type: boolean
                  listingType:
                    description: The publisher category to which the publication belongs.
                      The publisher category informs where the listing appears for
                      use.
                    type: string
                  longDescription:
                    description: (Updatable) A long description of the publication
                      to use in the listing.
                    type: string
                  name:
                    description: (Updatable) The name of the publication, which is
                      also used in the listing.
                    type: string
                  packageDetails:
                    description: A base object for creating a publication package.
                    items:
                      properties:
                        eula:
                          description: The end user license agreeement (EULA) that
                            consumers of this listing must accept.
                          items:
                            properties:
                              eulaType:
                                description: The end user license agreement's type.
                                type: string
                              licenseText:
                                description: The text of the end user license agreement.
                                type: string
                            type: object
                          type: array
                        imageId:
                          description: The unique identifier for the base image of
                            the publication.
                          type: string
                        imageIdRef:
                          description: Reference to a Image in compute to populate
                            imageId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        imageIdSelector:
                          description: Selector for a Image in compute to populate
                            imageId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        operatingSystem:
                          description: The operating system used by the listing.
                          items:
                            properties:
                              name:
                                description: (Updatable) The name of the publication,
                                  which is also used in the listing.
                                type: string
                            type: object
                          type: array
                        packageType:
                          description: The package's type.
                          type: string
                        packageVersion:
                          description: The package version.
                          type: string
                      type: object
                    type: array
                  shortDescription:
                    description: (Updatable) A short description of the publication
                      to use in the listing.
                    type: string
                  supportContacts:
                    description: (Updatable) Contact information for getting support
                      from the publisher for the listing.
                    items:
                      properties:
                        email:
                          description: (Updatable) The email of the contact.
                          type: string
                        name:
                          description: (Updatable) The name of the publication, which
                            is also used in the listing.
                          type: string
                        phone:
                          description: (Updatable) The phone number of the contact.
                          type: string
                        subject:
                          description: (Updatable) The email subject line to use when
                            contacting support.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  compartmentId:
                    description: (Updatable) The OCID of the compartment where you
                      want to create the publication.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) The defined tags associated with this
                      resource, if any. Each key is predefined and scoped to namespaces.
                      For more information, see Resource Tags. Example: {"Operations.CostCenter":
                      "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) The freeform tags associated with this
                      resource, if any. Each tag is a simple key-value pair with no
                      predefined name, type, or namespace. For more information, see
                      Resource Tags. Example: {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  isAgreementAcknowledged:
                    description: Whether the publisher acknowledged that they have
                      the right and authority to share the contents of the publication
                      and that they accepted the Oracle terms of use agreements required
                      to create a publication.
                    type: boolean
                  listingType:
                    description: The publisher category to which the publication belongs.
                      The publisher category informs where the listing appears for
                      use.
                    type: string
                  longDescription:
                    description: (Updatable) A long description of the publication
                      to use in the listing.
                    type: string
                  name:
                    description: (Updatable) The name of the publication, which is
                      also used in the listing.
                    type: string
                  packageDetails:
                    description: A base object for creating a publication package.
                    items:
                      properties:
                        eula:
                          description: The end user license agreeement (EULA) that
                            consumers of this listing must accept.
                          items:
                            properties:
                              eulaType:
                                description: The end user license agreement's type.
                                type: string
                              licenseText:
                                description: The text of the end user license agreement.
                                type: string
                            type: object
                          type: array
                        imageId:
                          description: The unique identifier for the base image of
                            the publication.
                          type: string
                        imageIdRef:
                          description: Reference to a Image in compute to populate
                            imageId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        imageIdSelector:
                          description: Selector for a Image in compute to populate
                            imageId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        operatingSystem:
                          description: The operating system used by the listing.
                          items:
                            properties:
                              name:
                                description: (Updatable) The name of the publication,
                                  which is also used in the listing.
                                type: string
                            type: object
                          type: array
                        packageType:
                          description: The package's type.
                          type: string
                        packageVersion:
                          description: The package version.
                          type: string
                      type: object
                    type: array
                  shortDescription:
                    description: (Updatable) A short description of the publication
                      to use in the listing.
                    type: string
                  supportContacts:
                    description: (Updatable) Contact information for getting support
                      from the publisher for the listing.
                    items:
                      properties:
                        email:
                          description: (Updatable) The email of the contact.
                          type: string
                        name:
                          description: (Updatable) The name of the publication, which
                            is also used in the listing.
                          type: string
                        phone:
                          description: (Updatable) The phone number of the contact.
                          type: string
                        subject:
                          description: (Updatable) The email subject line to use when
                            contacting support.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.isAgreementAcknowledged is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.isAgreementAcknowledged)
                || (has(self.initProvider) && has(self.initProvider.isAgreementAcknowledged))'
            - message: spec.forProvider.listingType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.listingType)
                || (has(self.initProvider) && has(self.initProvider.listingType))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.packageDetails is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.packageDetails)
                || (has(self.initProvider) && has(self.initProvider.packageDetails))'
            - message: spec.forProvider.shortDescription is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.shortDescription)
                || (has(self.initProvider) && has(self.initProvider.shortDescription))'
            - message: spec.forProvider.supportContacts is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.supportContacts)
                || (has(self.initProvider) && has(self.initProvider.supportContacts))'
          status:
            description: PublicationStatus defines the observed state of Publication.
            properties:
              atProvider:
                properties:
                  compartmentId:
                    description: (Updatable) The OCID of the compartment where you
                      want to create the publication.
                    type: string
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) The defined tags associated with this
                      resource, if any. Each key is predefined and scoped to namespaces.
                      For more information, see Resource Tags. Example: {"Operations.CostCenter":
                      "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) The freeform tags associated with this
                      resource, if any. Each tag is a simple key-value pair with no
                      predefined name, type, or namespace. For more information, see
                      Resource Tags. Example: {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  icon:
                    description: The model for upload data for images and icons.
                    items:
                      properties:
                        contentUrl:
                          description: The content URL of the upload data.
                          type: string
                        fileExtension:
                          description: The file extension of the upload data.
                          type: string
                        mimeType:
                          description: The MIME type of the upload data.
                          type: string
                        name:
                          description: (Updatable) The name of the publication, which
                            is also used in the listing.
                          type: string
                      type: object
                    type: array
                  id:
                    description: The unique identifier for the publication in Marketplace.
                    type: string
                  isAgreementAcknowledged:
                    description: Whether the publisher acknowledged that they have
                      the right and authority to share the contents of the publication
                      and that they accepted the Oracle terms of use agreements required
                      to create a publication.
                    type: boolean
                  listingType:
                    description: The publisher category to which the publication belongs.
                      The publisher category informs where the listing appears for
                      use.
                    type: string
                  longDescription:
                    description: (Updatable) A long description of the publication
                      to use in the listing.
                    type: string
                  name:
                    description: (Updatable) The name of the publication, which is
                      also used in the listing.
                    type: string
                  packageDetails:
                    description: A base object for creating a publication package.
                    items:
                      properties:
                        eula:
                          description: The end user license agreeement (EULA) that
                            consumers of this listing must accept.
                          items:
                            properties:
                              eulaType:
                                description: The end user license agreement's type.
                                type: string
                              licenseText:
                                description: The text of the end user license agreement.
                                type: string
                            type: object
                          type: array
                        imageId:
                          description: The unique identifier for the base image of
                            the publication.
                          type: string
                        operatingSystem:
                          description: The operating system used by the listing.
                          items:
                            properties:
                              name:
                                description: (Updatable) The name of the publication,
                                  which is also used in the listing.
                                type: string
                            type: object
                          type: array
                        packageType:
                          description: The package's type.
                          type: string
                        packageVersion:
                          description: The package version.
                          type: string
                      type: object
                    type: array
                  packageType:
                    description: The package's type.
                    type: string
                  shortDescription:
                    description: (Updatable) A short description of the publication
                      to use in the listing.
                    type: string
                  state:
                    description: The lifecycle state of the publication.
                    type: string
                  supportContacts:
                    description: (Updatable) Contact information for getting support
                      from the publisher for the listing.
                    items:
                      properties:
                        email:
                          description: (Updatable) The email of the contact.
                          type: string
                        name:
                          description: (Updatable) The name of the publication, which
                            is also used in the listing.
                          type: string
                        phone:
                          description: (Updatable) The phone number of the contact.
                          type: string
                        subject:
                          description: (Updatable) The email subject line to use when
                            contacting support.
                          type: string
                      type: object
                    type: array
                  supportedOperatingSystems:
                    description: The list of operating systems supported by the listing.
                    items:
                      properties:
                        name:
                          description: (Updatable) The name of the publication, which
                            is also used in the listing.
                          type: string
                      type: object
                    type: array
                  systemTags:
                    additionalProperties:
                      type: string
                    description: 'The system tags associated with this resource, if
                      any. The system tags are set by Oracle Cloud Infrastructure
                      services. Each key is predefined and scoped to namespaces. For
                      more information, see Resource Tags. Example: {orcl-cloud: {free-tier-retain:
                      true}}'
                    type: object
                    x-kubernetes-map-type: granular
                  timeCreated:
                    description: 'The date and time the publication was created, expressed
                      in RFC 3339 timestamp format.  Example: 2016-08-25T21:10:29.600Z'
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
