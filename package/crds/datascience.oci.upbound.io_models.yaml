---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: models.datascience.oci.upbound.io
spec:
  group: datascience.oci.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: Model
    listKind: ModelList
    plural: models
    singular: model
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Model is the Schema for the Models API. Provides the Model resource
          in Oracle Cloud Infrastructure Data Science service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelSpec defines the desired state of Model
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  artifactContentDisposition:
                    description: 'This allows to specify a filename during upload.
                      This file name is used to dispose of the file contents while
                      downloading the file. Example: attachment; filename=model-artifact.zip'
                    type: string
                  artifactContentLength:
                    description: The content length of the model_artifact.
                    type: string
                  backupSetting:
                    description: (Updatable) Back up setting details of the model.
                    items:
                      properties:
                        backupRegion:
                          description: (Updatable) Oracle Cloud Infrastructure backup
                            region for the model.
                          type: string
                        customerNotificationType:
                          description: (Updatable) Customer notification on backup
                            success/failure events.
                          type: string
                        isBackupEnabled:
                          description: (Updatable) Boolean flag representing whether
                            backup needs to be enabled/disabled for the model.
                          type: boolean
                      type: object
                    type: array
                  compartmentId:
                    description: (Updatable) The OCID of the compartment to create
                      the model in.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  customMetadataList:
                    description: (Updatable) An array of custom metadata details for
                      the model.
                    items:
                      properties:
                        category:
                          description: (Updatable) Category of model metadata which
                            should be null for defined metadata.For custom metadata
                            is should be one of the following values "Performance,Training
                            Profile,Training and Validation Datasets,Training Environment,Reports,Readme,other".
                          type: string
                        description:
                          description: (Updatable) Description of model metadata
                          type: string
                        hasArtifact:
                          description: (Updatable) Is there any artifact present for
                            the metadata.
                          type: boolean
                        key:
                          description: '(Updatable) Key of the model Metadata. The
                            key can either be user defined or Oracle Cloud Infrastructure
                            defined. List of Oracle Cloud Infrastructure defined keys:'
                          type: string
                        keywords:
                          description: (Updatable) list of keywords for searching
                          items:
                            type: string
                          type: array
                        value:
                          description: '(Updatable) Allowed values for useCaseType:
                            binary_classification, regression, multinomial_classification,
                            clustering, recommender, dimensionality_reduction/representation,
                            time_series_forecasting, anomaly_detection, topic_modeling,
                            ner, sentiment_analysis, image_classification, object_localization,
                            other'
                          type: string
                      type: object
                    type: array
                  definedMetadataList:
                    description: (Updatable) An array of defined metadata details
                      for the model.
                    items:
                      properties:
                        category:
                          description: (Updatable) Category of model metadata which
                            should be null for defined metadata.For custom metadata
                            is should be one of the following values "Performance,Training
                            Profile,Training and Validation Datasets,Training Environment,Reports,Readme,other".
                          type: string
                        description:
                          description: (Updatable) Description of model metadata
                          type: string
                        hasArtifact:
                          description: (Updatable) Is there any artifact present for
                            the metadata.
                          type: boolean
                        key:
                          description: '(Updatable) Key of the model Metadata. The
                            key can either be user defined or Oracle Cloud Infrastructure
                            defined. List of Oracle Cloud Infrastructure defined keys:'
                          type: string
                        keywords:
                          description: (Updatable) list of keywords for searching
                          items:
                            type: string
                          type: array
                        value:
                          description: '(Updatable) Allowed values for useCaseType:
                            binary_classification, regression, multinomial_classification,
                            clustering, recommender, dimensionality_reduction/representation,
                            time_series_forecasting, anomaly_detection, topic_modeling,
                            ner, sentiment_analysis, image_classification, object_localization,
                            other'
                          type: string
                      type: object
                    type: array
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. See Resource Tags.
                      Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) Description of model metadata
                    type: string
                  displayName:
                    description: '(Updatable) A user-friendly display name for the
                      resource. It does not have to be unique and can be modified.
                      Avoid entering confidential information. Example: My Model'
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Free-form tags for this resource. Each
                      tag is a simple key-value pair with no predefined name, type,
                      or namespace. See Resource Tags. Example: {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  inputSchema:
                    description: Input schema file content in String format
                    type: string
                  modelArtifact:
                    description: The model artifact to upload. It is a ZIP archive
                      of the files necessary to run the model. This can be done in
                      a separate step or using cli/sdk. The Model will remain in "Creating"
                      state until its artifact is uploaded.
                    type: string
                  modelVersionSetId:
                    description: The OCID of the model version set that the model
                      is associated to.
                    type: string
                  modelVersionSetName:
                    description: The name of the model version set that the model
                      is associated to.
                    type: string
                  outputSchema:
                    description: Output schema file content in String format
                    type: string
                  projectId:
                    description: The OCID of the project to associate with the model.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in datascience to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in datascience to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  retentionSetting:
                    description: (Updatable) Retention setting details of the model.
                    items:
                      properties:
                        archiveAfterDays:
                          description: (Updatable) Number of days after which the
                            model will be archived.
                          type: number
                        customerNotificationType:
                          description: (Updatable) Customer notification on backup
                            success/failure events.
                          type: string
                        deleteAfterDays:
                          description: (Updatable) Number of days after which the
                            archived model will be deleted.
                          type: number
                      type: object
                    type: array
                  state:
                    description: The state of the model.
                    type: string
                  versionLabel:
                    description: (Updatable) The version label can add an additional
                      description of the lifecycle state of the model or the application
                      using/training the model.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  artifactContentDisposition:
                    description: 'This allows to specify a filename during upload.
                      This file name is used to dispose of the file contents while
                      downloading the file. Example: attachment; filename=model-artifact.zip'
                    type: string
                  artifactContentLength:
                    description: The content length of the model_artifact.
                    type: string
                  backupSetting:
                    description: (Updatable) Back up setting details of the model.
                    items:
                      properties:
                        backupRegion:
                          description: (Updatable) Oracle Cloud Infrastructure backup
                            region for the model.
                          type: string
                        customerNotificationType:
                          description: (Updatable) Customer notification on backup
                            success/failure events.
                          type: string
                        isBackupEnabled:
                          description: (Updatable) Boolean flag representing whether
                            backup needs to be enabled/disabled for the model.
                          type: boolean
                      type: object
                    type: array
                  compartmentId:
                    description: (Updatable) The OCID of the compartment to create
                      the model in.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  customMetadataList:
                    description: (Updatable) An array of custom metadata details for
                      the model.
                    items:
                      properties:
                        category:
                          description: (Updatable) Category of model metadata which
                            should be null for defined metadata.For custom metadata
                            is should be one of the following values "Performance,Training
                            Profile,Training and Validation Datasets,Training Environment,Reports,Readme,other".
                          type: string
                        description:
                          description: (Updatable) Description of model metadata
                          type: string
                        hasArtifact:
                          description: (Updatable) Is there any artifact present for
                            the metadata.
                          type: boolean
                        key:
                          description: '(Updatable) Key of the model Metadata. The
                            key can either be user defined or Oracle Cloud Infrastructure
                            defined. List of Oracle Cloud Infrastructure defined keys:'
                          type: string
                        keywords:
                          description: (Updatable) list of keywords for searching
                          items:
                            type: string
                          type: array
                        value:
                          description: '(Updatable) Allowed values for useCaseType:
                            binary_classification, regression, multinomial_classification,
                            clustering, recommender, dimensionality_reduction/representation,
                            time_series_forecasting, anomaly_detection, topic_modeling,
                            ner, sentiment_analysis, image_classification, object_localization,
                            other'
                          type: string
                      type: object
                    type: array
                  definedMetadataList:
                    description: (Updatable) An array of defined metadata details
                      for the model.
                    items:
                      properties:
                        category:
                          description: (Updatable) Category of model metadata which
                            should be null for defined metadata.For custom metadata
                            is should be one of the following values "Performance,Training
                            Profile,Training and Validation Datasets,Training Environment,Reports,Readme,other".
                          type: string
                        description:
                          description: (Updatable) Description of model metadata
                          type: string
                        hasArtifact:
                          description: (Updatable) Is there any artifact present for
                            the metadata.
                          type: boolean
                        key:
                          description: '(Updatable) Key of the model Metadata. The
                            key can either be user defined or Oracle Cloud Infrastructure
                            defined. List of Oracle Cloud Infrastructure defined keys:'
                          type: string
                        keywords:
                          description: (Updatable) list of keywords for searching
                          items:
                            type: string
                          type: array
                        value:
                          description: '(Updatable) Allowed values for useCaseType:
                            binary_classification, regression, multinomial_classification,
                            clustering, recommender, dimensionality_reduction/representation,
                            time_series_forecasting, anomaly_detection, topic_modeling,
                            ner, sentiment_analysis, image_classification, object_localization,
                            other'
                          type: string
                      type: object
                    type: array
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. See Resource Tags.
                      Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) Description of model metadata
                    type: string
                  displayName:
                    description: '(Updatable) A user-friendly display name for the
                      resource. It does not have to be unique and can be modified.
                      Avoid entering confidential information. Example: My Model'
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Free-form tags for this resource. Each
                      tag is a simple key-value pair with no predefined name, type,
                      or namespace. See Resource Tags. Example: {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  inputSchema:
                    description: Input schema file content in String format
                    type: string
                  modelArtifact:
                    description: The model artifact to upload. It is a ZIP archive
                      of the files necessary to run the model. This can be done in
                      a separate step or using cli/sdk. The Model will remain in "Creating"
                      state until its artifact is uploaded.
                    type: string
                  modelVersionSetId:
                    description: The OCID of the model version set that the model
                      is associated to.
                    type: string
                  modelVersionSetName:
                    description: The name of the model version set that the model
                      is associated to.
                    type: string
                  outputSchema:
                    description: Output schema file content in String format
                    type: string
                  projectId:
                    description: The OCID of the project to associate with the model.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in datascience to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in datascience to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  retentionSetting:
                    description: (Updatable) Retention setting details of the model.
                    items:
                      properties:
                        archiveAfterDays:
                          description: (Updatable) Number of days after which the
                            model will be archived.
                          type: number
                        customerNotificationType:
                          description: (Updatable) Customer notification on backup
                            success/failure events.
                          type: string
                        deleteAfterDays:
                          description: (Updatable) Number of days after which the
                            archived model will be deleted.
                          type: number
                      type: object
                    type: array
                  state:
                    description: The state of the model.
                    type: string
                  versionLabel:
                    description: (Updatable) The version label can add an additional
                      description of the lifecycle state of the model or the application
                      using/training the model.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.artifactContentLength is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.artifactContentLength)
                || (has(self.initProvider) && has(self.initProvider.artifactContentLength))'
            - message: spec.forProvider.modelArtifact is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.modelArtifact)
                || (has(self.initProvider) && has(self.initProvider.modelArtifact))'
          status:
            description: ModelStatus defines the observed state of Model.
            properties:
              atProvider:
                properties:
                  artifactContentDisposition:
                    description: 'This allows to specify a filename during upload.
                      This file name is used to dispose of the file contents while
                      downloading the file. Example: attachment; filename=model-artifact.zip'
                    type: string
                  artifactContentLength:
                    description: The content length of the model_artifact.
                    type: string
                  artifactContentMd5:
                    type: string
                  artifactLastModified:
                    type: string
                  backupOperationDetails:
                    description: Backup operation details of the model.
                    items:
                      properties:
                        backupState:
                          description: The backup status of the model.
                          type: string
                        backupStateDetails:
                          description: The backup execution status details of the
                            model.
                          type: string
                        timeLastBackup:
                          description: The last backup execution time of the model.
                          type: string
                      type: object
                    type: array
                  backupSetting:
                    description: (Updatable) Back up setting details of the model.
                    items:
                      properties:
                        backupRegion:
                          description: (Updatable) Oracle Cloud Infrastructure backup
                            region for the model.
                          type: string
                        customerNotificationType:
                          description: (Updatable) Customer notification on backup
                            success/failure events.
                          type: string
                        isBackupEnabled:
                          description: (Updatable) Boolean flag representing whether
                            backup needs to be enabled/disabled for the model.
                          type: boolean
                      type: object
                    type: array
                  category:
                    description: (Updatable) Category of model metadata which should
                      be null for defined metadata.For custom metadata is should be
                      one of the following values "Performance,Training Profile,Training
                      and Validation Datasets,Training Environment,Reports,Readme,other".
                    type: string
                  compartmentId:
                    description: (Updatable) The OCID of the compartment to create
                      the model in.
                    type: string
                  createdBy:
                    description: The OCID of the user who created the model.
                    type: string
                  customMetadataList:
                    description: (Updatable) An array of custom metadata details for
                      the model.
                    items:
                      properties:
                        category:
                          description: (Updatable) Category of model metadata which
                            should be null for defined metadata.For custom metadata
                            is should be one of the following values "Performance,Training
                            Profile,Training and Validation Datasets,Training Environment,Reports,Readme,other".
                          type: string
                        description:
                          description: (Updatable) Description of model metadata
                          type: string
                        hasArtifact:
                          description: (Updatable) Is there any artifact present for
                            the metadata.
                          type: boolean
                        key:
                          description: '(Updatable) Key of the model Metadata. The
                            key can either be user defined or Oracle Cloud Infrastructure
                            defined. List of Oracle Cloud Infrastructure defined keys:'
                          type: string
                        keywords:
                          description: (Updatable) list of keywords for searching
                          items:
                            type: string
                          type: array
                        value:
                          description: '(Updatable) Allowed values for useCaseType:
                            binary_classification, regression, multinomial_classification,
                            clustering, recommender, dimensionality_reduction/representation,
                            time_series_forecasting, anomaly_detection, topic_modeling,
                            ner, sentiment_analysis, image_classification, object_localization,
                            other'
                          type: string
                      type: object
                    type: array
                  definedMetadataList:
                    description: (Updatable) An array of defined metadata details
                      for the model.
                    items:
                      properties:
                        category:
                          description: (Updatable) Category of model metadata which
                            should be null for defined metadata.For custom metadata
                            is should be one of the following values "Performance,Training
                            Profile,Training and Validation Datasets,Training Environment,Reports,Readme,other".
                          type: string
                        description:
                          description: (Updatable) Description of model metadata
                          type: string
                        hasArtifact:
                          description: (Updatable) Is there any artifact present for
                            the metadata.
                          type: boolean
                        key:
                          description: '(Updatable) Key of the model Metadata. The
                            key can either be user defined or Oracle Cloud Infrastructure
                            defined. List of Oracle Cloud Infrastructure defined keys:'
                          type: string
                        keywords:
                          description: (Updatable) list of keywords for searching
                          items:
                            type: string
                          type: array
                        value:
                          description: '(Updatable) Allowed values for useCaseType:
                            binary_classification, regression, multinomial_classification,
                            clustering, recommender, dimensionality_reduction/representation,
                            time_series_forecasting, anomaly_detection, topic_modeling,
                            ner, sentiment_analysis, image_classification, object_localization,
                            other'
                          type: string
                      type: object
                    type: array
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. See Resource Tags.
                      Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) Description of model metadata
                    type: string
                  displayName:
                    description: '(Updatable) A user-friendly display name for the
                      resource. It does not have to be unique and can be modified.
                      Avoid entering confidential information. Example: My Model'
                    type: string
                  emptyModel:
                    type: boolean
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Free-form tags for this resource. Each
                      tag is a simple key-value pair with no predefined name, type,
                      or namespace. See Resource Tags. Example: {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: The OCID of the model.
                    type: string
                  inputSchema:
                    description: Input schema file content in String format
                    type: string
                  isModelByReference:
                    description: Identifier to indicate whether a model artifact resides
                      in the Service Tenancy or Customer Tenancy.
                    type: boolean
                  lifecycleDetails:
                    description: Details about the lifecycle state of the model.
                    type: string
                  modelArtifact:
                    description: The model artifact to upload. It is a ZIP archive
                      of the files necessary to run the model. This can be done in
                      a separate step or using cli/sdk. The Model will remain in "Creating"
                      state until its artifact is uploaded.
                    type: string
                  modelVersionSetId:
                    description: The OCID of the model version set that the model
                      is associated to.
                    type: string
                  modelVersionSetName:
                    description: The name of the model version set that the model
                      is associated to.
                    type: string
                  outputSchema:
                    description: Output schema file content in String format
                    type: string
                  projectId:
                    description: The OCID of the project to associate with the model.
                    type: string
                  retentionOperationDetails:
                    description: Retention operation details for the model.
                    items:
                      properties:
                        archiveState:
                          description: The archival status of model.
                          type: string
                        archiveStateDetails:
                          description: The archival state details of the model.
                          type: string
                        deleteState:
                          description: The deletion status of the archived model.
                          type: string
                        deleteStateDetails:
                          description: The deletion status details of the archived
                            model.
                          type: string
                        timeArchivalScheduled:
                          description: The estimated archival time of the model based
                            on the provided retention setting.
                          type: string
                        timeDeletionScheduled:
                          description: The estimated deletion time of the model based
                            on the provided retention setting.
                          type: string
                      type: object
                    type: array
                  retentionSetting:
                    description: (Updatable) Retention setting details of the model.
                    items:
                      properties:
                        archiveAfterDays:
                          description: (Updatable) Number of days after which the
                            model will be archived.
                          type: number
                        customerNotificationType:
                          description: (Updatable) Customer notification on backup
                            success/failure events.
                          type: string
                        deleteAfterDays:
                          description: (Updatable) Number of days after which the
                            archived model will be deleted.
                          type: number
                      type: object
                    type: array
                  state:
                    description: The state of the model.
                    type: string
                  timeCreated:
                    description: 'The date and time the resource was created in the
                      timestamp format defined by RFC3339. Example: 2019-08-25T21:10:29.41Z'
                    type: string
                  versionLabel:
                    description: (Updatable) The version label can add an additional
                      description of the lifecycle state of the model or the application
                      using/training the model.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
