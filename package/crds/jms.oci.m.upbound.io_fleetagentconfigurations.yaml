---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: fleetagentconfigurations.jms.oci.m.upbound.io
spec:
  group: jms.oci.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: FleetAgentConfiguration
    listKind: FleetAgentConfigurationList
    plural: fleetagentconfigurations
    singular: fleetagentconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FleetAgentConfiguration is the Schema for the FleetAgentConfigurations
          API. Provides the Fleet Agent Configuration resource in Oracle Cloud Infrastructure
          Jms service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FleetAgentConfigurationSpec defines the desired state of
              FleetAgentConfiguration
            properties:
              forProvider:
                properties:
                  agentPollingIntervalInMinutes:
                    description: (Updatable) Agent polling interval in minutes
                    type: number
                  fleetId:
                    description: The OCID of the Fleet.
                    type: string
                  fleetIdRef:
                    description: Reference to a Fleet in jms to populate fleetId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  fleetIdSelector:
                    description: Selector for a Fleet in jms to populate fleetId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isCapturingIpAddressAndFqdnEnabled:
                    description: (Updatable) Collect network addresses of managed
                      instances in the fleet.
                    type: boolean
                  isCollectingManagedInstanceMetricsEnabled:
                    description: (Updatable) Collect JMS agent metrics on all managed
                      instances in the fleet.
                    type: boolean
                  isCollectingUsernamesEnabled:
                    description: (Updatable) Collect username for application invocations
                      for all managed instances in the fleet.
                    type: boolean
                  isLibrariesScanEnabled:
                    description: (Updatable) Enable libraries scan on all managed
                      instances in the fleet.
                    type: boolean
                  javaUsageTrackerProcessingFrequencyInMinutes:
                    description: (Updatable) The frequency (in minutes) of Java Usage
                      Tracker processing. (That is, how often should JMS process data
                      from the Java Usage Tracker.)
                    type: number
                  jreScanFrequencyInMinutes:
                    description: (Updatable) The frequency (in minutes) of JRE scanning.
                      (That is, how often should JMS scan for JRE installations.)
                    type: number
                  linuxConfiguration:
                    description: (Updatable) Management Agent Configuration for list
                      of include/exclude file system paths (specific to operating
                      system).
                    items:
                      properties:
                        excludePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                        includePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  macOsConfiguration:
                    description: (Updatable) Management Agent Configuration for list
                      of include/exclude file system paths (specific to operating
                      system).
                    items:
                      properties:
                        excludePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                        includePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  windowsConfiguration:
                    description: (Updatable) Management Agent Configuration for list
                      of include/exclude file system paths (specific to operating
                      system).
                    items:
                      properties:
                        excludePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                        includePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  workRequestValidityPeriodInDays:
                    description: (Updatable) The validity period in days for work
                      requests.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  agentPollingIntervalInMinutes:
                    description: (Updatable) Agent polling interval in minutes
                    type: number
                  fleetId:
                    description: The OCID of the Fleet.
                    type: string
                  fleetIdRef:
                    description: Reference to a Fleet in jms to populate fleetId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  fleetIdSelector:
                    description: Selector for a Fleet in jms to populate fleetId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isCapturingIpAddressAndFqdnEnabled:
                    description: (Updatable) Collect network addresses of managed
                      instances in the fleet.
                    type: boolean
                  isCollectingManagedInstanceMetricsEnabled:
                    description: (Updatable) Collect JMS agent metrics on all managed
                      instances in the fleet.
                    type: boolean
                  isCollectingUsernamesEnabled:
                    description: (Updatable) Collect username for application invocations
                      for all managed instances in the fleet.
                    type: boolean
                  isLibrariesScanEnabled:
                    description: (Updatable) Enable libraries scan on all managed
                      instances in the fleet.
                    type: boolean
                  javaUsageTrackerProcessingFrequencyInMinutes:
                    description: (Updatable) The frequency (in minutes) of Java Usage
                      Tracker processing. (That is, how often should JMS process data
                      from the Java Usage Tracker.)
                    type: number
                  jreScanFrequencyInMinutes:
                    description: (Updatable) The frequency (in minutes) of JRE scanning.
                      (That is, how often should JMS scan for JRE installations.)
                    type: number
                  linuxConfiguration:
                    description: (Updatable) Management Agent Configuration for list
                      of include/exclude file system paths (specific to operating
                      system).
                    items:
                      properties:
                        excludePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                        includePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  macOsConfiguration:
                    description: (Updatable) Management Agent Configuration for list
                      of include/exclude file system paths (specific to operating
                      system).
                    items:
                      properties:
                        excludePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                        includePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  windowsConfiguration:
                    description: (Updatable) Management Agent Configuration for list
                      of include/exclude file system paths (specific to operating
                      system).
                    items:
                      properties:
                        excludePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                        includePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  workRequestValidityPeriodInDays:
                    description: (Updatable) The validity period in days for work
                      requests.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: FleetAgentConfigurationStatus defines the observed state
              of FleetAgentConfiguration.
            properties:
              atProvider:
                properties:
                  agentPollingIntervalInMinutes:
                    description: (Updatable) Agent polling interval in minutes
                    type: number
                  fleetId:
                    description: The OCID of the Fleet.
                    type: string
                  id:
                    type: string
                  isCapturingIpAddressAndFqdnEnabled:
                    description: (Updatable) Collect network addresses of managed
                      instances in the fleet.
                    type: boolean
                  isCollectingManagedInstanceMetricsEnabled:
                    description: (Updatable) Collect JMS agent metrics on all managed
                      instances in the fleet.
                    type: boolean
                  isCollectingUsernamesEnabled:
                    description: (Updatable) Collect username for application invocations
                      for all managed instances in the fleet.
                    type: boolean
                  isLibrariesScanEnabled:
                    description: (Updatable) Enable libraries scan on all managed
                      instances in the fleet.
                    type: boolean
                  javaUsageTrackerProcessingFrequencyInMinutes:
                    description: (Updatable) The frequency (in minutes) of Java Usage
                      Tracker processing. (That is, how often should JMS process data
                      from the Java Usage Tracker.)
                    type: number
                  jreScanFrequencyInMinutes:
                    description: (Updatable) The frequency (in minutes) of JRE scanning.
                      (That is, how often should JMS scan for JRE installations.)
                    type: number
                  linuxConfiguration:
                    description: (Updatable) Management Agent Configuration for list
                      of include/exclude file system paths (specific to operating
                      system).
                    items:
                      properties:
                        excludePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                        includePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  macOsConfiguration:
                    description: (Updatable) Management Agent Configuration for list
                      of include/exclude file system paths (specific to operating
                      system).
                    items:
                      properties:
                        excludePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                        includePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  timeLastModified:
                    description: The date and time of the last modification to the
                      Fleet Agent Configuration (formatted according to RFC3339).
                    type: string
                  windowsConfiguration:
                    description: (Updatable) Management Agent Configuration for list
                      of include/exclude file system paths (specific to operating
                      system).
                    items:
                      properties:
                        excludePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                        includePaths:
                          description: (Updatable) An array of file system paths (environment
                            variables supported).
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  workRequestValidityPeriodInDays:
                    description: (Updatable) The validity period in days for work
                      requests.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
