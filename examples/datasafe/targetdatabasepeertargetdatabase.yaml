apiVersion: datasafe.oci.upbound.io/v1alpha1
kind: TargetDatabasePeerTargetDatabase
metadata:
  annotations:
    meta.upbound.io/example-id: datasafe/v1alpha1/targetdatabasepeertargetdatabase
  labels:
    testing.upbound.io/example-name: test_target_database_peer_target_database
  name: test-target-database-peer-target-database
spec:
  forProvider:
    databaseDetails:
    - databaseType: DATABASE_CLOUD_SERVICE
      dbSystemId: ${var.db_system_id}
      infrastructureType: ORACLE_CLOUD
      listenerPort: ${var.listener_port}
      serviceName: ${var.service_name}
    description: peer target database description
    displayName: peerTargetDatabase1
    targetDatabaseId: ${var.data_safe_adg_target_ocid}
